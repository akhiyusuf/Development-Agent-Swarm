# Pipeline Review Log

## 2026-07-10 — Stage: research-design — Verdict: REJECTED

**Reviewed output:** `research/design-research.md`
**Dependencies:** none (checked instead against `.claude/agents/researcher-design.md` output requirements and `docs/idea.md`, since the researcher agent reported it could not read the idea doc this session).

### Requirement coverage (agent spec)

| Requirement | Status |
|---|---|
| Competitor Visual Audit (3-5 competitors/adjacent) | Present — 9 products audited (MyFitnessPal, YAZIO, Cronometer, Calistree, Calistack, Thenx, Duolingo, Habitica, Flutterwave), each with a concrete design lesson |
| Interaction Conventions | Present but incomplete (see gaps 1-2) |
| Accessibility Requirements | Present and strong — WCAG specifics plus audience-specific low-bandwidth/low-end-device constraints |
| Recommended Direction (with rationale) | Present — correctly framed as a recommendation; cultural-identity question honestly flagged as unresolved rather than papered over |

Honesty check: passes. The doc explicitly discloses it worked from the task description instead of `docs/idea.md`, and flags its open questions instead of inventing answers. That disclosure was warranted — it caused a material gap.

### Concrete gaps (reasons for rejection)

1. **Pilates is entirely missing.** `docs/idea.md` differentiator #2 states the workout system "Covers calisthenics and Pilates specifically." The research's own scope line describes only "calisthenics/bodyweight skill-tree progression"; zero Pilates or Pilates-adjacent apps are audited, and no Pilates interaction conventions appear anywhere. Pilates apps follow a materially different UX register (guided/timed mat sessions, video- or audio-led flows, form-cue-heavy instruction) than rep-based calisthenics skill trees. Since `design-system` depends only on this stage, Pilates conventions would never enter the design system at all. This is not a nice-to-have — it is half of the workout product.
2. **No in-workout execution conventions.** The Interaction Conventions section covers the skill-tree *map* (node states, navigation) but not the workout *session* itself: rest timers, rep counters, hold-duration timers (directly required by the idea's "time/rep thresholds they've hit" tracking), mid-exercise glanceability/one-hand use, and session-complete flows. These are among the highest-frequency screens in the product and have strong category conventions worth documenting.
3. **(Minor, fix alongside the above) Portion-entry conventions for African dishes.** Differentiator #1 (African ingredient/prepared-dish database, logging "by individual ingredient or by common prepared meal") has a design-facing consequence the doc misses: many African staples are served/measured in household units (wraps, ladles, handfuls) rather than gram weights, which affects the serving-size UI convention the doc otherwise describes in mainstream (gram/barcode) terms. A short note on this is enough; deep sourcing belongs to product research.

### Required changes for approval

1. Add Pilates to scope and audit 1-2 Pilates or Pilates-adjacent apps (e.g., Lottie Pilates, 5 Minute Pilates, Alo Moves, Nike Training Club Pilates content) for visual identity and session UX.
2. Add an Interaction Conventions subsection for guided workout-session execution: timer-led holds, rep logging, rest timers, audio/video guidance patterns, and how these conventions differ between calisthenics and Pilates.
3. Add a brief note on portion/serving-entry conventions for prepared African dishes (household measures vs. grams) as a design-system input for the logging flow.
4. Update the scope statement and Recommended Direction to confirm the "warm/clean hybrid" recommendation still holds (or is adjusted) once Pilates session UX is factored in.

What does NOT need to change: the competitor audit for nutrition/skill-tree/gamification/African-brand quadrants, the accessibility section, and the honestly-flagged cultural-identity decision are all solid and should be preserved as-is.

## 2026-07-10 — Stage: research-product — Verdict: REJECTED

**Reviewed output:** `research/product-research.md`
**Dependencies:** none (checked against `.claude/agents/researcher-product.md` output requirements and reconciled against `docs/idea.md`, since the researcher agent reported it could not read the idea doc this session and worked from the launcher brief).

### Requirement coverage (agent spec)

| Requirement | Status |
|---|---|
| Target Users + jobs-to-be-done | Present — three personas with a clear primary wedge and four JTBD |
| Competitive Landscape (3-5, gaps + complaints) | Present and strong on nutrition (MyFitnessPal, Cronometer, CalorieNaija, Didiye, AfriCal, AfroTools, FitSavanna, with sourced complaint evidence); **wrong category on the workout side** (see gap 2) |
| Core Feature Requirements (viable minimum) | Present, well-scoped on nutrition; workout requirements contradict the idea (gaps 1-2) and micronutrients are omitted (gap 3) |
| Constraints & Risks (regulatory/technical/market) | Present and strong — NDPR/GDPR, offline-first/low-end Android, data-curation labor cost, monetization realism |
| Open Questions | Present — but drops one of the idea doc's own open questions (gap 4) |

Honesty check: passes strongly. The "whitespace is filling fast" competitive finding, the self-reported-AI-accuracy skepticism, and the explicit "Weak spots to be honest about" subsection all push back on the idea rather than validating it. The missing-idea-doc disclosure at the top is also honest — and, as with research-design, that disclosure flagged a real problem.

### Idea-doc reconciliation (checked directly against `docs/idea.md`)

Handled correctly: Nigeria-mandatory-in-v1 honored; region-prioritization open question answered (Nigeria + Ghana + Kenya with data-cost rationale); data-sourcing open question answered (WAFCT 2019, Nigerian FCT, Kenya FCT 2018, USDA, with the composite-dish caveat); unified-vs-two-module open question answered with a clear recommendation; mobile-first/diaspora constraints reflected.

### Concrete gaps (reasons for rejection)

1. **Calisthenics and Pilates are entirely absent.** The words appear nowhere in the document, yet they are in the title of `docs/idea.md`, and differentiator #2 states the workout system "Covers calisthenics and Pilates specifically." Pilates in particular has zero coverage — no persona, no competitor, no feature requirement, no risk. This is the same failure that contributed to the research-design rejection, and since `sitemap` depends only on this stage, Pilates would never enter the information architecture at all.
2. **The workout concept is contradicted, not just under-covered.** The idea describes a bodyweight skill-tree: moves gated behind prerequisite mastery (e.g., freestanding handstand hold before handstand push-ups), tracking "current unlocked tier and the time/rep thresholds they've hit." The research reframes this as a generic gym progressive-overload product: a "gym-goer / progressive-overload lifter" persona, a "tell me what to do in the gym" JTBD, a competitor set of Fitbod/Dr. Muscle/JEFIT (weights apps), and feature requirements specifying "load/sets," "sets/reps/weight," and "gym paths." The idea mentions no gym and no external load. Freeletics is the only on-concept comparison in the doc. (Note: validating the exercise-science correctness of progression trees was out of scope for this stage — the failure here is misrepresenting the product concept, not failing to validate it.)
3. **Micronutrient tracking is omitted from Core Feature Requirements.** Differentiator #1 in `docs/idea.md` explicitly requires "Micronutrient data (vitamins, minerals) where available." The feature list covers only calories + macros (req 4). The cited FCT sources do carry micronutrient data, so this is a cheap fix, but as written the sitemap stage would build no micronutrient surface.
4. **The idea doc's open question on validated progression trees is dropped.** `docs/idea.md` explicitly lists "the actual correct, safe progression trees for calisthenics and Pilates, validated against real strength-training and Pilates progression standards" as an open research question. The doc's Open Questions section does not carry it forward (the generic "reconcile with idea.md" item, Q6, is not a substitute). Downstream stages need to know this is unresolved and owned by someone.

### Required changes for approval

1. Rewrite the workout-module coverage around the actual concept: calisthenics + Pilates bodyweight skill-tree with mastery gates and time/rep thresholds. Remove or correct the gym/weights framing in the tertiary persona, JTBD 3, and feature requirements 6-8 (no "loads," "weight," or "gym paths" unless justified against the idea doc).
2. Add on-category workout competitors: at least one calisthenics skill-tree app (e.g., Thenx, Calisthenics Mastery, Movesta/Calistree — the design-research audit already found some) and at least one Pilates app, with gaps/complaints as done for the nutrition side. Freeletics can stay as the progression-gating analogue.
3. Add micronutrient tracking (per-food vitamins/minerals where the FCT source provides them) to Core Feature Requirements, noting FCT micronutrient coverage limits as a constraint if applicable.
4. Add the calisthenics/Pilates progression-tree validation to Open Questions as an explicitly unresolved item with a proposed owner/stage (it is flagged in `docs/idea.md` as a core research task, not an assumption).
5. Reconcile or remove the top-of-file "brief-based" disclaimer and Open Q6 once the above is done against the actual `docs/idea.md` contents.

What does NOT need to change: the entire nutrition-side analysis (personas, competitive landscape, data-sourcing resolution, portion-sizing problem framing), the Nigeria+Ghana+Kenya market recommendation, the unified-app/nutrition-first recommendation, the Constraints & Risks section, and the honesty posture are all strong and should be preserved as-is.

## 2026-07-10 — Stage: research-design (SECOND PASS) — Verdict: REJECTED

**Reviewed output:** `research/design-research.md` (revision after first-pass rejection)
**Dependencies:** none (checked against `.claude/agents/researcher-design.md` output requirements, `docs/idea.md`, and the first-pass fix list above).

### First-pass fix list: all four items verified DONE

| Required change (pass 1) | Status in revision |
|---|---|
| 1. Pilates in scope + 1-2 Pilates apps audited for visual identity and session UX | **Done.** Three apps audited (Alo Moves, 5 Minute Pilates, Nike Training Club Pilates content), each covering both visual identity and session UX, plus an explicit register contrast vs. calisthenics apps. |
| 2. In-workout execution conventions for both modes | **Done.** Separate calisthenics subsection (rep counters, rest timers, hold-duration timers, one-hand glanceability, session-complete tied to skill-tree nodes) and Pilates subsection (timed guided flow, audio-first cues, rest folded into transitions, reflective session-complete), with differences drawn explicitly and a concrete design implication (one shared session-player component, mode-specific tone). |
| 3. Household-unit vs. gram portion-entry note for African dishes | **Done.** Correctly scoped as a UI-convention input (household-unit pickers primary, gram/barcode secondary) with data sourcing explicitly deferred to product research. |
| 4. Scope statement + Recommended Direction updated | **Done.** Scope note covers both modes; Recommended Direction explicitly confirms warm/clean hybrid with an added tonal-flex requirement, and re-flags design-system-architect ownership. |

### Concrete gaps (reasons for rejection): reviewed-and-approved content was silently removed, and the doc claims it wasn't

The first-pass verdict explicitly instructed: "the competitor audit for nutrition/skill-tree/gamification/African-brand quadrants, the accessibility section, and the honestly-flagged cultural-identity decision are all solid and should be preserved as-is." Checked against the first pass's recorded audit list (MyFitnessPal, YAZIO, Cronometer, Calistree, Calistack, Thenx, Duolingo, Habitica, Flutterwave):

1. **The gamification quadrant (Duolingo, Habitica) is entirely gone.** No gamification-native product audit remains anywhere in the revision. The achievement/unlock/streak layer is core to `docs/idea.md` differentiator #2, and `design-system` depends only on this document for those lessons; the residual unlock/badge notes inside the skill-tree section are not a substitute for the removed audit entries and their design lessons.
2. **The African-brand quadrant lost its named reference (Flutterwave)**, replaced by an unnamed generic description ("African recipe blogs/Instagram accounts, diaspora meal-kit brands"). An audit entry with no named product is not auditable or traceable.
3. **YAZIO was silently swapped for Noom** in the nutrition quadrant. Noom is a defensible (arguably better) warm-register example, but the change is unacknowledged.
4. **The scope note misrepresents the above.** Line 3 states: "Everything else from the prior draft (nutrition/skill-tree/gamification/African-brand competitor quadrants, the accessibility section, and the open cultural-identity question) is preserved as reviewed." That claim is false for two of the four quadrants (gamification removed, African-brand reference stripped) and inaccurate for a third (nutrition entry swapped). The prior draft's honesty was one of its strengths; this is a regression on that axis, and it is the kind of claim downstream agents will take at face value.

Preserved correctly: the accessibility section (changed only additively — new in-workout, audio/haptic, and cognitive-load items are improvements tied to the required fixes), the cultural-identity open question (still honestly flagged as a design-system-stage decision), and the skill-tree map conventions.

### Required changes for approval (narrow — do not touch the four new additions)

1. Restore the gamification-app audit entries (Duolingo and Habitica, each with its concrete design lesson as in the prior draft). If the reviser believes they should be cut, that must be argued explicitly, not deleted silently.
2. Restore the named African/diaspora brand reference (Flutterwave, or another named brand with a concrete design lesson) in place of the current unnamed generic description.
3. Either restore YAZIO or keep Noom and note the swap as a deliberate, reasoned change.
4. Correct the scope note so it no longer claims unchanged preservation of content that was removed or altered.

**Explicitly approved as-is (must NOT change in the next revision):** the Pilates audit section, both in-workout execution subsections, the portion-entry note, the updated Recommended Direction with tonal flex, and the (additively improved) accessibility section.

## 2026-07-10 — Stage: research-product (SECOND PASS) — Verdict: APPROVED

**Reviewed output:** `research/product-research.md` (revision after first-pass rejection)
**Dependencies:** none (checked against `.claude/agents/researcher-product.md` output requirements, `docs/idea.md`, and the first-pass fix list above).

### First-pass fix list: all five items verified DONE

| Required change (pass 1) | Status in revision |
|---|---|
| 1. Rewrite workout coverage around calisthenics + Pilates bodyweight skill-tree with mastery gates; remove gym/weights framing | **Done.** Scope statement now states "no gym, no machines, and no external load" explicitly. Two on-concept workout personas (home/outdoor bodyweight trainer; Pilates-lean mobility/core trainer) replace the gym-goer. JTBD 3 is now the named-skill progression job. Feature requirements 6-11 specify a calisthenics+Pilates skill tree, objective time-hold/rep-threshold gates, per-user unlocked-tier state, placement onboarding, form cues, and session logging. Fitbod/Dr. Muscle/JEFIT and all "load/sets"/"weight"/"gym paths" language are gone; "external-load/gym tracking" is explicitly declared out of scope (req list, final paragraph). The only remaining "weight" mentions are body-weight goals and a criticism of Calistree's weight-vest drift — both legitimate. |
| 2. On-category workout competitors with gaps/complaints | **Done.** Calistree and Calisthenics Mastery (skill-tree calisthenics), Thenx, Freeletics retained as the progression-gating analogue (with the sharp observation that its gates are self-rated — directly motivating the idea's objective-threshold differentiator), and Pilatesology + Pilates Anytime for Pilates. Each row carries specific complaints (beginner overwhelm, confusing layout, self-rated gates, "I don't know where to start") sourced to reviews/articles at the same standard as the nutrition table, plus a market-read paragraph synthesizing the wedge. |
| 3. Micronutrient tracking in Core Feature Requirements | **Done.** Requirement 4: per-food and per-day vitamins/minerals (iron, zinc, calcium, vitamin A, folate, B12 named), tied to FCT sourcing feasibility, with graceful "no data" degradation; incomplete-FCT-coverage risk carried into Constraints and Open Question 4 as required. |
| 4. Progression-tree validation carried into Open Questions | **Done.** Open Question 1 explicitly flags it as "unresolved, needs dedicated exercise-science validation — out of scope here, must not be assumed," names the owner type (qualified coach/physiotherapist/Pilates instructor) and timing (before sitemap/progression build), and rejects adopting Convict Conditioning as a shortcut. Also echoed in the injury-liability constraint and honest-weak-spots. |
| 5. Remove stale brief-based disclaimer and old Open Q6 | **Done.** The doc now opens grounded directly in `docs/idea.md`; the generic reconcile-with-idea.md open question is gone, replaced by substantive questions. |

### Preservation check (explicitly verified for silent thinning, given the research-design pass-2 failure mode)

Compared against the first-pass review's recorded description of the approved content — depth, not just keyword presence:

- **Diaspora persona:** intact as persona 2, including the dual-cuisine scope-multiplier insight ("serving them well requires the Western food database to also be solid — a scope multiplier, not a free extension"), the hard-currency willingness-to-pay point, and the Didiye/CalorieNaija-courting observation. The insight is additionally carried into Constraints & Risks and Open Question 7 (dual-cuisine IA implications). Not thinned.
- **Recommendation on Priority Markets:** full section intact — Nigeria mandatory (population, documented pain, WAFCT + national FCT, Nigerian diaspora), Ghana second on shared-WAFCT low-marginal-data-cost rationale, Kenya third on its own lab-analyzed 2018 FCT (522 foods + 142 recipes) and East-African beachhead, with South Africa and Ethiopia explicitly held for v2 with per-country reasoning and the closing curation-cost-per-cuisine rationale. Not thinned.
- **Unified-app vs. two-modules:** full rationale intact and improved — one app, two modules sharing account/profile/energy-balance layer, nutrition-first sequencing with justification (persona overlap only partial; workout module blocked on tree validation), shared calories-in/out model as the integration payoff, explicit warning against a split two-app architecture. Not a one-line reference.
- **Everything else the first pass marked preserve-as-is** (nutrition competitive table with sourced complaint evidence, data-sourcing resolution with WAFCT/Nigerian FCT/Kenya FCT/USDA and the composite-dish + licensing caveats, Constraints & Risks incl. NDPR/GDPR, offline-first/low-end Android, curation-labor moat/cost, monetization realism, and the honest-weak-spots posture) is present at equal or greater depth.

### Agent-spec coverage

All five required sections present (Target Users, Competitive Landscape, Core Feature Requirements, Constraints & Risks, Open Questions); honesty requirement satisfied strongly (whitespace-closing finding, unvalidated-differentiator warning, bundle-risk warning); closing statement present.

**Verdict: APPROVED.** No gaps found. Sitemap stage is unblocked; it must treat Open Question 1 (progression-tree validation) as an unresolved external dependency, not settled fact.

## 2026-07-10 — Stage: sitemap — Verdict: REJECTED

**Reviewed output:** `docs/sitemap.md`
**Dependencies:** `research-product` (approved, pass 2) → `research/product-research.md`. Checked every one of the 13 numbered Core Feature Requirements against the traceability table and the actual screen list, plus the three special conditions carried forward from the research approval.

### What passes (verified, not vibes)

- **Requirements 2–8 and 10–12 trace fully and correctly** from the traceability table to real screens in the hierarchy (spot-checked each: e.g., Req 2's Search/Recent/Favorites/Custom tabs all exist on Add Entry; Req 7's threshold display, Log Attempt, and Mastery Gate Confirmation all exist; Req 4's "no data" degradation appears on Daily Nutrition Summary, the Home widget, and per-food previews; Req 12's shared account/profile/weight/goal layer plus Combined Progress Dashboard is present).
- **Req 13 is handled honestly**: the screen-expressible slice (low-data toggle, manual sync, Health/Fit integrations) is placed, and the rest is correctly flagged as a cross-cutting engineering constraint rather than falsely claimed as a screen. This is the right call, and the flag is explicit.
- **Open Question 1 discipline: correct.** The scope note carries the unvalidated-progression-tree caveat forward verbatim in intent; nodes are `[Skill Node]` placeholders; thresholds are "values TBD by validated content"; no exercise names, orderings, or specific thresholds are invented anywhere in the workout section. The assessment's movement-pattern categories (push/pull/squat-hinge/core-hold) are marked "generic, content TBD," which is acceptable — but see gap 2 on what the assessment structurally outputs.
- **Unified-app structure: correct.** One bottom tab bar; shared Home (energy-balance card), Progress (combined dashboard), and Profile layers; single auth/profile onboarding; the nutrition-first sequencing is correctly expressed as a rollout note ("coming soon" workout tab) rather than a structural split. No two-disconnected-trees failure.
- **Screens without a numbered requirement** (Module Interest, Home dashboard, Legal & Disclaimers) are all explicitly justified in the Flags section against the research's sequencing recommendation and Constraints & Risks (injury liability, NDPR/GDPR positioning). Justified, not scope creep.
- **Deliberately-out-of-scope features** (social, barcode, AI photo, meal planning, gym/external load) are correctly absent.

### Concrete gaps (reasons for rejection)

1. **Req 1 is claimed as fully covered in the traceability table, but its portion-photo reference set is missing from the sitemap entirely.** Requirement 1 reads: real household-unit portions "each mapped to gram weights internally, **plus a portion-photo reference set** — this is the entry-point that mainstream apps fail." The word "photo" appears nowhere in `docs/sitemap.md`. This is not below sitemap granularity: the Ingredient Detail and Composite Meal Detail screens already enumerate portion-selector contents at widget level (household units, gram equivalent, macro/micro preview), so the photo reference was omitted at the document's own operating granularity. The research independently names portion realism the #1 still-open market gap (swallows, submerged solids, shared pots; the Nairobi portion-photo-atlas citation). And unlike Req 13, where partial coverage was honestly flagged, traceability row 1 asserts full coverage with no flag — so the screens stage would take "Req 1 done" at face value and never design it.
2. **Req 9's placement assessment does not structurally account for the Pilates track.** The assessment steps are calisthenics-flavored movement patterns (push/pull/squat-hinge/core-hold) and the result screen is a singular "Starting Tier Placement," while the skill tree has two distinct tracks (Calisthenics, Pilates — Skill Tree Home's own track selector). Req 9 exists to solve the "overwhelming / I don't know where to start" complaint, which the research documents *specifically for Pilates apps* (Pilatesology/Pilates Anytime: "I don't know where to start"). As written, the screens stage could reasonably build a calisthenics-only assessment that leaves a Pilates-lean user (research persona 5, possibly Pilates-only) unplaced — reproducing the exact incumbent failure. The fix is structural, not content: the assessment/results screens must state that placement is per-track (or that each track has its own placement path), while keeping the actual assessment content TBD per Open Question 1.

### Required changes for approval (narrow — everything else should be preserved as-is)

1. Add the portion-photo reference set to the sitemap: either as an element of the Ingredient Detail / Composite Meal Detail portion selector (e.g., "portion-photo reference for the selected household unit") and/or as a Portion Reference Guide screen/sheet reachable from the portion selector — architect's choice — and update traceability row 1 to name it.
2. Clarify the Workout Placement Assessment structure so both tracks get an entry point: state that Assessment Results / Starting Tier Placement is per-track (calisthenics placement and Pilates placement), or that each track has its own short placement flow. Keep all assessment *content* generic/TBD as currently done. Update traceability row 9 accordingly.

**Explicitly approved as-is (must NOT change in revision):** the five-tab navigation structure, the onboarding sequence, all nutrition screens (beyond the additive fix 1), the entire workout tier/node/gate screen set and its placeholder discipline, the Progress and Profile/Settings sections, the modal inventory, the Flags section's honest handling of Req 13 and the three requirement-less screens, and the deferred-features list. Per this pipeline's history: do not silently remove or thin anything while making the two fixes above.

## 2026-07-10 — Stage: sitemap (SECOND PASS) — Verdict: APPROVED

**Reviewed output:** `docs/sitemap.md` (revision after first-pass rejection)
**Dependencies:** `research-product` (approved, pass 2 — status re-confirmed in `pipeline/state.json`) → `research/product-research.md`. Checked against the first-pass fix list above and, item by item, against the first pass's explicitly-approved-as-is preservation list.

### First-pass fix list: both items verified DONE

| Required change (pass 1) | Status in revision |
|---|---|
| 1. Portion-photo reference set for Req 1, named in traceability row 1 | **Done, at three levels.** (a) Ingredient Detail now carries an inline portion-photo reference displayed alongside the household-unit picker, with the research's "entry-point differentiator / #1 still-open gap" rationale correctly cited; (b) Composite Meal Detail explicitly states it uses "the same portion-selector pattern including the portion-photo reference"; (c) a new **Portion Reference Guide** sheet/modal (expanded photo set — multiple angles / small-to-heaping range) is reachable from the portion selector on both detail screens, is listed in the modal inventory, and appears in the Navigation Hierarchy Summary's nested and modal listings. Traceability row 1 no longer over-claims: it names the portion-photo reference set in the requirement text and lists all three surfaces (Ingredient Detail, Composite Meal Detail, Portion Reference Guide). |
| 2. Per-track placement assessment (calisthenics and Pilates each get an entry point), content kept generic/TBD; traceability row 9 updated | **Done, structurally.** Assessment Intro now states up front that placement is per-track; a **Track Selection** step lets the user take Calisthenics placement, Pilates placement, or both (either order), with deferral supported and a later entry point from Skill Tree Home (the tree-side hook is present on the Skill Tree Home screen itself, not just asserted). Results are per-track: separate "Calisthenics Starting Tier Placement" and "Pilates Starting Tier Placement" screens replace the singular undifferentiated placement, with a conditional Combined Assessment Summary when both are completed. The Navigation Hierarchy Summary's pre-nav line reflects the new structure. Traceability row 9 states the per-track requirement and lists all five assessment screens plus the deferred-placement entry point. |

### Open Question 1 discipline re-verified after the fix

The Pilates placement steps are described as "e.g., core/breath control, mobility, mat-position tolerance; generic, content TBD" — the same abstraction level as the already-approved calisthenics movement-pattern categories (push/pull/squat-hinge/core-hold, still marked "generic, content TBD"), and the doc explicitly disclaims asserting the criteria ("not asserting the same criteria, since Pilates placement is a materially different discipline"). No exercise names, node orderings, or specific thresholds are invented anywhere; `[Skill Node]` placeholders and "values TBD by validated content" remain intact. Acceptable — the fix is structural, as instructed, not content invention.

### Preservation check (item by item against the first pass's approved-as-is list — this pipeline's documented silent-thinning failure mode)

- **Five-tab bottom nav:** intact (Home, Nutrition, Workout, Progress, Profile/Settings; hierarchy summary unchanged).
- **Onboarding sequence:** intact in full (Splash → Sign Up/Log In with forgot-password → Profile Setup → Goal & Target Setup → Region & Cuisine Preference → Module Interest → conditional Workout Placement Assessment → Onboarding Complete/Welcome). The assessment restructure is the required fix, not thinning; all prior steps survive inside it.
- **Nutrition screens:** all intact — Food Diary (Today), Add Entry with all four tabs (Search/Recent/Favorites/Custom), Food Search Results, Ingredient Detail, Composite Meal Detail, Custom Food/Meal Builder, Confirm & Log, Edit/Delete Entry, Daily Nutrition Summary, Micronutrient Detail, Nutrition History/Calendar, Favorites & Recents Management. Changes are strictly additive (photo reference + new guide sheet).
- **Workout tier/node/gate set and placeholder discipline:** all intact — Skill Tree Home (track selector, plus the additive deferred-placement note), Tier/Node Map with locked/in-progress/mastered states and prerequisites, Node Detail with `[Skill Node]` placeholder and TBD thresholds and form cues, Log Attempt, Mastery Gate Confirmation, Progression Status, Workout Session Log, Workout History/Session Calendar.
- **Progress:** intact (Combined Progress Dashboard with the cross-module payoff framing, Weight Log, Goal Settings/Adjust Targets).
- **Profile/Settings:** all eight screens intact, including Data & Sync Settings (low-data toggle, manual sync), Integrations, and Legal & Disclaimers with the liability/positioning content.
- **Modal inventory:** intact plus one additive entry (Portion Reference Guide).
- **Flags section:** intact — the three requirement-less screens with their justifications, the honest Req 13 cross-cutting flag, and the full deferred-features list.
- **Scope note:** the Open Question 1 unvalidated-progression-trees caveat and mechanism-only framing carried forward intact.
- **Traceability rows 2–8, 10–13:** spot-checked against the first pass's recorded descriptions — unchanged.

Nothing was removed or thinned; every change is either one of the two required fixes or a directly supporting addition.

**Verdict: APPROVED.** Screens stage is unblocked on the sitemap side (it still also depends on `design-system`, which remains rejected/pending). Carry-forwards for the screens stage: (1) treat Open Question 1 as unresolved — design assessment/node screens around placeholder content, do not invent exercises or thresholds; (2) the portion-photo reference is a first-class element of the portion selector, not decoration — the research names it the market's #1 still-open gap; (3) placement is per-track and deferrable — screens must handle the one-track, both-track, and deferred states.

## 2026-07-10 — Stage: research-design (THIRD PASS) — Verdict: APPROVED

**Reviewed output:** `research/design-research.md` (revision after second-pass rejection; missing content merged back by the orchestrator)
**Dependencies:** none (checked against `.claude/agents/researcher-design.md` output requirements, `docs/idea.md`, the pass-1 fix list, and the pass-2 restoration list above).

### Pass-2 restoration list: all four items verified DONE

| Required change (pass 2) | Status in this revision |
|---|---|
| 1. Restore Duolingo + Habitica gamification-audit entries with their concrete design lessons | **Done.** The "Gamification design language" section is back with both entries: Duolingo (fixed color-to-meaning semantic mapping — green/red/orange/yellow/purple — with the lesson that gamified state legibility requires an explicit color vocabulary in the design system) and Habitica (full "game skin" as a contrast/cautionary case for a health product aimed at a broad adult diaspora audience). Both match the substance the pass-1 review recorded and the Recommended Direction still cites both by name in its rationale (Duolingo-style fixed meanings; Habitica-style over-gaming risk), so the entries are load-bearing, not decorative. |
| 2. Restore the named African/diaspora brand reference (Flutterwave) with a concrete design lesson | **Done.** Flutterwave is back as a named, first-class audit entry (vivid orange/green/pink identity as a deliberate break from fintech blue-green defaults, warm/community positioning aimed at African users' aspirations). The previously offending unnamed generic paragraph (recipe blogs / meal-kit brands) now *supplements* the named entry rather than replacing it, alongside an honestly-hedged note on other African fintechs (Paystack, Moniepoint, Chipper Cash) that discloses thin sourcing rather than overclaiming. Flutterwave is also still referenced in the audit takeaway and the Recommended Direction, so the named reference is traceable end to end. |
| 3. Either restore YAZIO or keep Noom with the swap noted | **Done via the first option.** YAZIO is restored in the nutrition quadrant with its original design lesson (card layouts, restrained per-macro color vocabulary, whitespace-as-premium); Noom — never-approved intermediate content — is gone, which is the natural consequence of the "restore YAZIO" branch of the fix instruction and requires no further acknowledgement. The audit now matches the pass-1 recorded nine-product list exactly (MyFitnessPal, YAZIO, Cronometer, Calistree, Calistack, Thenx, Duolingo, Habitica, Flutterwave) plus the pass-1-required Pilates trio. |
| 4. Correct the scope note's false preservation claim | **Done.** The revision note now states the opposite of the pass-2 falsehood: it explicitly acknowledges that the Duolingo/Habitica quadrant and the Flutterwave reference were "mistakenly dropped in an intermediate revision pass" and have been restored, and it accurately enumerates the four additions (Pilates audit, in-workout conventions, portion-entry note, tonal-flex addendum). Checked line by line against the document: every claim in the note is true of the current contents. The honesty regression is repaired. |

### Pass-1 fixes re-verified intact (per pass 2's explicit must-NOT-change list)

All four are present and unchanged in substance from what pass 2 verified and approved: (1) the Pilates audit (Alo Moves, 5 Minute Pilates, NTC Pilates content) with the calmer/softer-register vs. game-like-register takeaway; (2) both in-workout execution subsections — calisthenics (rep counters, rest timers, hold-duration timers, arm's-length glanceability, node-tied session-complete) and Pilates (timed audio-led flow, rest folded into transitions, reflective session-complete) — with the shared reskinnable session-player design implication; (3) the household-unit portion-entry note, still correctly scoped as a UI convention with data sourcing deferred to product research; (4) the Recommended Direction still confirms the warm/clean hybrid "after factoring in Pilates session UX" with the tonal-flex addendum and design-system-architect ownership re-flagged.

### Originally-approved content re-verified

- **Accessibility section:** intact, including pass 2's approved additive improvements (in-workout contrast/legibility, audio/haptic state cues, single-focus cognitive-load guidance) and the audience-specific low-bandwidth/low-end-device/offline-first constraints.
- **Cultural-identity open question:** intact and still honestly flagged — both directions (universal/neutral vs. deliberately pan-African) presented with real costs on each side, explicitly deferred to the design-system-architect with a call for community input rather than researcher inference.
- **Skill-tree map conventions and nutrition/logging conventions:** intact, including the Pilates-content-maps-to-the-same-tree note.

### Agent-spec coverage

All four required sections present (Competitor Visual Audit, Interaction Conventions, Accessibility Requirements, Recommended Direction with rationale); direction correctly framed as a recommendation with ownership deferred; closing statement present. Alignment with `docs/idea.md` re-confirmed: both differentiators (African ingredient/prepared-dish logging incl. household portions; calisthenics **and** Pilates progression-gated skill tree with time/rep thresholds) now have design-research coverage, and no progression content is invented (consistent with the idea's validation caveat).

**Verdict: APPROVED.** No gaps found. Design-system stage is unblocked. Carry-forwards for design-system: (1) define an explicit gamification color-to-meaning vocabulary (Duolingo lesson) scoped to skill-tree state, never color-only (accessibility requirement); (2) one shared session-player component with mode-specific tonal styling (game-like calisthenics vs. calm audio-led Pilates), not two player UIs; (3) household-unit portion pickers as the primary serving-size convention, grams/barcode secondary; (4) the cultural-identity direction (universal/neutral vs. pan-African-warm) is an unresolved decision this stage now owns — decide it explicitly and record the rationale; (5) low-bandwidth/low-end-device constraints rule out heavy illustration/animation-led systems regardless of aesthetic choice.

## 2026-07-10 — Stage: design-system — Verdict: REJECTED

**Reviewed output:** `docs/design-system.md`
**Dependencies:** `research-design` (approved, pass 3 — status re-confirmed in `pipeline/state.json`) → `research/design-research.md`. Checked against the five carry-forwards recorded in the research-design third-pass approval, the research document itself, and the `.claude/agents/design-system-architect.md` output requirements.

### What passes (verified, not vibes)

- **Carry-forward 2 — session-player tonal flex: substantially done.** §4.8 + §5 define one shared component with two style presets, exactly per the research ("shared components with mode-specific styling rather than two separate design languages"): calisthenics = dark, high-contrast, game-like (64pt `type.timer-xl`, 96px one-handed rep target, achievement-toned session-complete tied to node state); Pilates = light, calm, audio-led (single countdown, no rep target, muted gold, auto-advance toggle, reflective session-complete that still surfaces threshold progress — matching the research's "keep both modes tied to the same gamification loop" note). §5's closing paragraph correctly pins what is shared vs. what differs. One gap: the rest-timer state is missing (see gap 3).
- **Carry-forward 3 — household-unit-first portion entry: done.** §4.3 makes the household-unit chip picker the explicit default for prepared-meal logging with gram/barcode entry demoted to a secondary "advanced/exact" path, plus a sensible no-conversion fallback state. §4.4 makes the portion-photo reference a first-class reusable component (inline tile + expanded Portion Reference Guide sheet, matching the approved sitemap's three surfaces), with offline/low-bandwidth image fallback treated as an expected state — consistent with both the research and the sitemap-review carry-forward.
- **Carry-forward 4 — cultural-identity decision: done.** §0 resolves the research's open judgment call explicitly (Option 2, restrained: warm pan-African color + authentic photography + copy tone; no decorative pattern/motif layer) with documented reasoning that engages the research's tokenism risk directly, notes the convergence with the performance constraint, and marks the decision revisitable with community input. This is a real, reasoned resolution, not a default.
- **Carry-forward 5 + accessibility: substantially done.** §6 covers all research requirements: 4.5:1/3:1 text contrast with a 7:1 target for in-workout numerals (research's "higher-than-minimum for in-workout" requirement), dynamic type to 200% with an honestly-disclosed clamp for `timer-xl`/`display`, 44x44pt targets with no exceptions, reduced-motion alternatives for every named animation and a no-flashing rule, color-never-alone (verified per-component: node states get icon/shape pairs, errors get icons, rings get labels), audio/haptic parity for workout state changes, single-focus in-workout screens, text-never-in-images, and performance-as-accessibility (solid colors, system fonts, no autoplay/GIF, compressed cached imagery, offline-first as a normal state). The palette is flat/solid, the font is the system stack, and cards default to borders over shadows — the low-bandwidth/low-end-device constraint is honored structurally, not just asserted.
- **Agent-spec output shape: complete.** Palette (primary/secondary/semantic/neutrals, dark+light) ✓; type scale (font, sizes, weights, line-heights) ✓; spacing/grid (4px base, tokens, 4-col grid, density modes per the research's two-rhythms note) ✓; component inventory with default/hover(press)/disabled/error for every entry, with reasoned N/A where a state genuinely doesn't apply ✓; accessibility rules ✓; closing statement present ✓; dependency approval precondition respected ✓.

### Concrete gaps (reasons for rejection)

1. **Carry-forward 1 fails on its own terms: the "fixed, exclusive" gamification color vocabulary is not actually fixed or exclusive, and the document claims it is.** §1.4 states its colors "are reserved exclusively for node/skill state and must not be reused for other UI purposes anywhere in the app." By the document's own hex tables this is false for four of five states: in-progress = `#2A6F97` is identical to `color.semantic.info` (§1.3); completed = `#2E7D46` is identical to `color.semantic.success` (§1.3); unlocked = `#D9A441` is `color.primary.gold`, whose §1.1 uses include "streak/XP-adjacent UI" and "secondary buttons"; mastered = `#C1502E` is `color.primary.terracotta` — the app-wide primary-button/active-tab/CTA color. The research's requirement (Duolingo lesson) was explicit: "color meaning fixed and consistent (not reused ad hoc for other UI purposes)" and "one color = locked, one = in-progress, one = mastered." A mastered node rendered in the same color as every primary button in the app is the opposite of a legible state vocabulary. Only the info/in-progress overlap is acknowledged (with a reserve hex, `#3B7EA8`, that the doc defines but then declines to use); the other three overlaps are presented as if they don't exist. Minor same-section defects to fix alongside: §1.4 says "four colors" but defines five states; §1.1's gold row cross-references "the fixed gamification vocabulary in §1.3" (it is §1.4).
2. **§4.10's macro-ring palette makes a distinctness claim that is factually false, compounding gap 1.** It asserts the per-macro colors are "distinct from both the brand palette's decorative use and the gamification vocabulary in §1.4 to avoid cross-contaminating meanings" — then assigns calories = terracotta (= mastered), carbs = gold (= unlocked), and fat = info-blue (= in-progress). Three of four macro rings reuse gamification-state hexes on the nutrition dashboard, the single highest-frequency screen in the product. This is precisely the ad-hoc reuse the research prohibits, stated under a claim of the opposite. Downstream agents take such claims at face value — this pipeline has already rejected two stages for exactly this pattern of false internal assertions.
3. **The calisthenics session-player preset omits the rest timer.** The research's in-workout calisthenics conventions (an approved, must-not-change section) name five elements; four are covered (rep counters, hold timers, glanceability, session-complete), but rest timers — "full-screen or near-full-screen countdown between sets, auto-starts on set completion, persists to lock screen/notification, skip/add-time controls" — appear nowhere in §4.8 or §5.1. The between-sets rest state is one of the highest-frequency in-session states and needs a defined place in the shared component (the audio/haptic "rest over" cue in §6.6 already presumes it exists).
4. **(Minor) §1.1 and §4.1 contradict each other on secondary buttons.** §1.1 lists "secondary buttons" as a use of `color.primary.gold`; §4.1 defines the secondary button as terracotta-outlined. A screen-designer can't follow both.
5. **(Minor) Non-text contrast is unaddressed, and at least one defined pairing fails it.** §6.1 covers text contrast only, but the system's core stateful UI is non-text: skill-tree node fills/rings and progress bars. WCAG 1.4.11 requires 3:1 for UI components; gold `#D9A441` on the light background `#F7F3EE` computes to roughly 2:1, and the dedicated `gold-muted #E0BE7C` Pilates progress indicator on the same background is lower still. The research's outdoor/glare-tolerance requirement makes this worse, not better. Icon/shape pairing (present, good) prevents a color-alone failure but does not make a ~2:1 ring visible in sunlight.
6. **(Minor) §4.3 mentions "barcode entry" as the secondary path, but the approved sitemap explicitly defers barcode scanning.** Not a traceability failure against this stage's own dependency (the research does treat barcode as a category baseline), but the doc calls itself "the single source of truth the screen-designer agent builds every screen from," and screens must follow the sitemap's scoping. Mark barcode as deferred (sitemap decision) or drop the word.

### Required changes for approval (narrow — everything else should be preserved as-is)

1. Make the §1.4 node-state vocabulary genuinely dedicated, or make its claims honest — no third option. Concretely: (a) use the already-defined reserve hex `#3B7EA8` (or another dedicated value) for in-progress instead of borrowing `info`; (b) give mastered and unlocked dedicated hexes that are not the app-wide primary/secondary brand accents, OR keep the shared values and rewrite the exclusivity claim into an explicit, reasoned scoping rule stating exactly where those hexes may appear outside the tree and why the contexts can't collide (completed = success-green may be defensible as meaning-aligned; mastered = every-CTA-terracotta is much harder to defend — if kept, argue it explicitly); (c) fix the "four colors"/five-states count and the §1.3-should-be-§1.4 cross-reference.
2. Fix §4.10: either assign macro-ring colors genuinely distinct from the (revised) gamification vocabulary, or delete the false "distinct from... the gamification vocabulary" claim and document the collision and why it's acceptable. The claim and the hex assignments cannot both stand as written. Keep the fixed one-color-per-macro principle (YAZIO lesson) either way.
3. Add the rest-timer state to the calisthenics preset in §5.1 (and the §4.8 component summary): auto-start on set completion, dominant countdown numeral, skip/add-time as secondary controls meeting the 44pt floor (the research calls these "small-touch-target" — §3's no-exceptions rule correctly overrides that), and a note that lock-screen/notification persistence is a build-stage behavior to carry forward.
4. Resolve the secondary-button color contradiction between §1.1 (gold) and §4.1 (terracotta) — one owner, one color.
5. Add a non-text contrast rule to §6 (3:1 for UI components/graphical state indicators per WCAG 1.4.11) and re-verify the gold-on-light pairings it currently fails (`#D9A441` and `#E0BE7C` on `#F7F3EE`), adjusting hexes or adding contrast-bearing borders where needed.
6. Mark barcode entry in §4.3 as deferred per the approved sitemap's scoping, or remove the mention.

**Explicitly approved as-is (must NOT change in revision):** §0 (cultural-identity decision and reasoning), §2 (type scale, system-font decision, timer-xl sizing and clamp disclosure), §3 (spacing, grid, touch targets, density modes), §4.3 and §4.4 (portion picker and portion-photo component, beyond the one-word barcode fix), §5's overall two-preset structure and everything in the Pilates preset, §6's existing nine rules (fix 5 is additive), and the neutrals/dark-mode policy. Per this pipeline's history: do not silently remove or thin anything while making the fixes above, and do not let the revision's prose claim more than the tables actually deliver — that mismatch is the primary reason for this rejection.

## 2026-07-10 — Stage: design-system (SECOND PASS) — Verdict: REJECTED

**Reviewed output:** `docs/design-system.md` (revision after first-pass rejection)
**Dependencies:** `research-design` (approved, pass 3 — status re-confirmed in `pipeline/state.json`) → `research/design-research.md`. Checked against the first-pass six-item fix list above, with every hex value cross-checked across sections and all contrast claims independently recomputed (WCAG relative-luminance formula) rather than taken on trust.

### First-pass fix list: five of six verified DONE, one half-done

| Required change (pass 1) | Status in revision |
|---|---|
| 1. Genuinely dedicated node-state vocabulary + count/cross-ref fixes | **Done.** All five node states now use dedicated hexes verified against every other hex in the document (brand §1.1, semantic §1.3, neutrals §1.2, macro rings §4.10, gold-dark, gold-muted): locked `#8A8578`, unlocked `#C98A2E` (≠ gold `#D9A441`), in-progress `#3B7EA8` (the pass-1 reserve hex, now actually used; ≠ info `#2A6F97`), completed `#3D8B5C` (≠ success `#2E7D46`, ≠ deepgreen `#1F5C42`), mastered `#A8452A` (≠ terracotta `#C1502E`, ≠ error `#B3271E`). The one intra-vocabulary reuse (mastered's outline = unlocked's amber) is disclosed and reasoned in-table. §1.4's exclusivity claim is now true of its own tables. "Five states" count fixed; §1.1's cross-reference now correctly points to §1.4. §5.1 additionally scopes node colors out of the live timer/rest view — a good additive guard. **But see gap 2: the new unlocked amber fails the new §6.10 contrast rule.** |
| 2. §4.10 macro-ring claim vs. hexes | **Done.** Rings are calories = terracotta `#C1502E`, protein = deepgreen `#1F5C42`, carbs = gold-dark `#A9761E`, fat = info `#2A6F97`. The false distinctness claim is replaced by an honest one: rings *deliberately* reuse brand/semantic hues (YAZIO small-vocabulary rationale, documented) and are distinct only from §1.4 — which is now factually true (zero hex overlap with the five node states, verified). One-color-per-macro fixed principle retained. Carbs correctly moved to the contrast-safe gold variant with the reasoning stated inline. |
| 3. Rest-timer state in §5.1/§4.8 | **Done.** §4.8 names the rest-timer state; §5.1 specifies it fully: auto-start on set-complete, dominant `type.timer-xl` countdown (white on dark, glanceable), skip/"+15s" secondary controls at the 44pt floor with the research's "small-touch-target" framing explicitly overridden by §3's no-exceptions rule, haptic on expiry with auto-transition back, and lock-screen/notification persistence correctly flagged as a build-stage behavior to carry forward. §6.3, §6.6, and §6.7 all now reference it consistently. |
| 4. Secondary-button color contradiction | **Done.** §1.1's gold row no longer lists secondary buttons; an explicit note in §1.1 and a matching note in §4.1 make the terracotta-outlined secondary button the single owner, with "gold is never used as a button's own color" stated in both places. No contradiction remains. |
| 5. Non-text contrast rule + failing gold pairings | **HALF-DONE — the unfixed half is a rejection gap (see gap 1).** The additive §6.10 rule (WCAG 1.4.11, 3:1 for UI components/state indicators) is correct and well-scoped. The `#D9A441` half is genuinely fixed: base gold is banned for stateful/interactive components, and the new `gold-dark #A9761E` independently recomputes to ~3.6:1 on `#F7F3EE` and ~3.2:1 on `#EDE6DC` — the doc's "verified above 3:1 against both light neutrals" claim is true, and §4.10's carbs ring correctly adopts it. But the **other pairing the fix list named by hex — `gold-muted #E0BE7C` — was not touched** (see gap 1). |
| 6. Barcode deferral | **Done.** §4.3 explicitly defers barcode-scan entry "per the approved sitemap's v1 scope," removes it as a v1 component, and correctly notes any future version would extend the secondary advanced/exact path. No other barcode mention remains in the document. |

### Concrete gaps (reasons for rejection — narrow: both are contrast failures against the doc's own new §6.10 rule)

1. **Fix 5 was only half-applied: the Pilates player's sole progress indicator still fails, exactly as named in the pass-1 fix list.** §5.2 still specifies `color.primary.gold-muted #E0BE7C` as "the single active-progress indicator" on the Pilates player's mandatory light surface (`warmgray-100 #F7F3EE`). Recomputed: **~1.6:1** against that background — far below §6.10's 3:1 minimum for "progress-ring/bar fills," which §6.10 itself lists — and **~1.0:1** (visually indistinguishable) against the standard `warmgray-400 #C9C0B2` progress track from §4.10. §6.10 bans base gold but is silent on gold-muted, so the document now simultaneously mandates 3:1 for progress fills and specifies a ~1.6:1 progress fill; a screen-designer cannot follow both. Pass 1's required change 5 named `#E0BE7C` on `#F7F3EE` explicitly ("adjusting hexes or adding contrast-bearing borders where needed") — no hex change, no border, and no reasoned exemption was added. (Acknowledged tension: pass 1 also listed the Pilates preset as preserve-as-is; the explicit named-hex fix instruction governs over the general preservation instruction, and the preservation list cannot grandfather a violation of a required fix.)
2. **The fix-1 replacement hex for unlocked was chosen without checking it against the fix-5 rule added in the same revision.** `color.node.unlocked #C98A2E` recomputes to **~2.7:1** on the app background `#F7F3EE` and **~2.4:1** on the card surface `#EDE6DC` — below §6.10's 3:1 minimum, which explicitly covers "node-state fills." The skill-tree map lives on light surfaces (§1.5: light is the default everywhere outside the calisthenics player), and unlocked is the state the whole progression loop funnels attention toward ("this is what you can do next"). The icon/ring pairing (present, good) satisfies §6.5's color-never-alone rule but does not satisfy §6.10, per §6.10's own text. The other four node hexes pass on `#F7F3EE` (in-progress ~4.0:1, completed ~3.8:1, mastered ~5.4:1, locked ~3.3:1 at base hex — though locked's 60% opacity treatment blends it below 3:1, that state is defensible as WCAG-inactive/exempt and should simply say so).

### Required changes for approval (narrow — everything else, including all six fix areas above where marked Done, must be preserved as-is)

1. **Resolve the §5.2 vs §6.10 contradiction on the Pilates progress indicator.** Options, architect's choice: (a) swap the indicator fill to `gold-dark #A9761E` (passes at ~3.6:1/~3.2:1; if the calm register needs softening, reduce the indicator's size/weight, not its contrast); (b) keep `#E0BE7C` as an inner fill but require a contrast-bearing border/track treatment that itself meets 3:1 and say so in both §5.2 and §6.10; or (c) a new muted-warm hex verified ≥3:1 against both `#F7F3EE` and the track color. Whatever the choice, §6.10 must name `gold-muted`'s permitted/forbidden uses the same way it now does for base gold, and the stated contrast figures must be real (this review recomputes them).
2. **Bring `color.node.unlocked` into compliance with §6.10 on light surfaces.** Either darken the dedicated amber (keeping it hex-distinct from `gold-dark #A9761E`, brand gold, and `semantic.warning #B8752A` — the vocabulary's dedication is now correct and must survive the adjustment) or specify a ≥3:1 contrast-bearing ring/border as the state's compliant indicator and document that in §1.4 and §6.10. While in §1.4, add the one-line note that locked's reduced-opacity rendering relies on the WCAG inactive-component exemption (or verify it, if not intended as exempt).
3. **(Optional, non-blocking, fix if touching those lines anyway):** §4.7's "gold outline" for boss/milestone nodes should name the hex — per §1.4 the mastered outline is the amber `#C98A2E`, and leaving the word "gold" invites a screen-designer to reach for `color.primary.gold`; §6.4 cites WCAG 2.5.4 for the no-flashing rule (flashing is 2.3.1; 2.5.4 is motion actuation) — carried over from the previously approved rule text, so not a rejection reason.

### Preservation check (item by item against the pass-1 approved-as-is list)

- **§0 cultural-identity decision:** intact — Option 2 restrained, tokenism reasoning, perf-constraint convergence, revisitable-with-community-input note, and the practical photography consequence all unchanged.
- **§2 type scale:** intact, including the system-font decision, `timer-xl` 64pt, and the honest 150% clamp disclosure.
- **§3 spacing/grid/touch-targets/density modes:** intact; the no-exceptions 44pt rule now correctly cited by the new rest-timer spec.
- **§4.3/§4.4 portion picker + portion-photo reference:** intact as first-class defaults; the only change is the required barcode-deferral fix, done in place without thinning the component specs or their offline/fallback states.
- **§5 two-preset structure and the Pilates preset:** intact; calisthenics preset changed only by the required rest-timer addition. (The Pilates preset's untouched state is itself gap 1 — preservation was honored where change was required.)
- **§6 rules 1–9:** intact verbatim in substance; rule 10 is additive as instructed; rules 3/6/7 gained only rest-timer references consistent with fix 3.
- **Neutrals and dark-mode policy (§1.2/§1.5):** intact.

Nothing was removed or thinned; every change traces to a required fix. The document's prose claims now match its tables everywhere except the two contrast items above — the honesty failure mode that drove the first rejection is repaired; what remains is a numeric compliance failure against the doc's own new rule, half of it explicitly named in the prior fix list.

**Verdict: REJECTED** — narrowly. Two required changes, both mechanical color/contrast resolutions; no structural, honesty, or coverage problems remain. Screens stage stays blocked on this stage.

## 2026-07-10 — Stage: design-system (THIRD PASS) — Verdict: APPROVED

**Reviewed output:** `docs/design-system.md` (revision after second-pass rejection)
**Dependencies:** `research-design` (approved, pass 3 — status re-confirmed in `pipeline/state.json`) → `research/design-research.md`. Checked against the pass-2 two-item fix list above. Every contrast figure below was independently recomputed from the WCAG relative-luminance formula, not taken from the document's own claims; the full-palette hex-exclusivity check was redone from scratch, not just for the two changed values.

### Pass-2 fix list: both items verified DONE, with all stated ratios independently confirmed

| Required change (pass 2) | Status in this revision |
|---|---|
| 1. Resolve §5.2 vs §6.10 on the Pilates progress indicator; §6.10 must name `gold-muted`'s permitted/forbidden uses; stated figures must be real | **Done via option (c) — a new darker hex.** `color.primary.gold-muted` is now `#7A4E12`, still scoped solely to the Pilates player's active-progress-indicator fill (§1.1 row, §5.2, §6.10). Independent recomputation: **6.50:1** vs. app background `#F7F3EE` (doc claims ~6.50 — confirmed), **5.79:1** vs. card `#EDE6DC` (claims ~5.80 — confirmed), **3.99:1** vs. the `warmgray-400 #C9C0B2` progress track (claims ~3.99 — confirmed). All clear §6.10's 3:1 floor, including the fill-vs-track pairing that was previously ~1.0:1 (invisible). §6.10 now names gold-muted explicitly, documents the old `#E0BE7C` failure with figures that match this review's own recomputation of that old value (1.61:1 vs. background, 1.01:1 vs. track), and bans the old hex outright ("must not be used anywhere in the system"). The §1.1 token row adds a correct use-restriction (progress-fill role only, not backgrounds/large fills). |
| 2. Bring `color.node.unlocked` into §6.10 compliance on light surfaces, preserving hex-dedication; add the locked-state inactive-exemption note to §1.4 | **Done via darkening.** `color.node.unlocked` is now `#A85F12`. Independent recomputation: **4.41:1** vs. `#F7F3EE` (doc claims ~4.41 — confirmed) and **3.93:1** vs. `#EDE6DC` (claims ~3.93 — confirmed). The doc's stated figures for the rejected prior value `#C98A2E` (~2.65/~2.37) also match this review's recomputation (2.66/2.37), so the §6.10 audit note is honest end to end. Additionally checked beyond the doc's own claims: the new hex also passes on the dark surfaces where §5.1 permits node colors in the post-session summary — **3.65:1** vs. `dark-bg #17181A` and **3.19:1** vs. `dark-surface #232427` — so the darkening did not silently break the dark-mode side. The mastered-node outline reference (§1.4) and §4.7's boss-node outline were both updated consistently to the new hex. The locked-state exemption note is present in §1.4 (and echoed in §6.10): base hex passes 3:1 in isolation, the 60%-opacity rendering is covered by the WCAG 1.4.11 inactive-component exemption, with an explicit re-verification condition if locked nodes ever become tappable. |

### Full hex-exclusivity re-verification (question 1)

`#A85F12` was checked against every hex in the document: brand (`#C1502E`, `#96391F`, `#D9A441`, `#A9761E`, `#7A4E12`, `#1F5C42`), semantic (`#2E7D46`, `#B3271E`, `#B8752A`, `#2A6F97`), all ten neutrals, the other four node states (`#8A8578`, `#3B7EA8`, `#3D8B5C`, `#A8452A`), and the macro-ring assignments (which are token references to terracotta/deepgreen/gold-dark/info). **Zero collisions.** Likewise `#7A4E12` appears nowhere else. §1.4's dedication claim remains true of the revised tables; the only same-hex reuse of `#A85F12` outside §1.4 is §4.7's boss-node outline, which is an explicit reference to §1.4's own mastered-outline spec (intra-vocabulary, disclosed), and §6.10's documentation of the value — not new UI roles.

### Design-coherence check on the darkened values (question 2)

- **`gold-muted #7A4E12` vs. `node.unlocked #A85F12`:** clearly hex- and luminance-distinct (relative luminance 0.096 vs. 0.166; HSL roughly 35°/74%/27% vs. 31°/81%/36%). They are in the same warm-brown family, but their contexts are disjoint by the doc's own rules: gold-muted exists only as the Pilates player's mid-session progress fill (where §5.2 bans §1.4 colors), and node colors appear on the tree map and session summaries. No near-duplicate problem in practice.
- **`node.unlocked #A85F12` vs. `gold-dark #A9761E`:** this is the closest pair in the revised palette (ΔL ≈ 3 points, Δhue ≈ 7°; the new unlocked is more copper/orange, gold-dark more olive). This proximity is the near-inevitable consequence of the pass-2 instruction itself — a dedicated amber that clears 3:1 on `#F7F3EE` necessarily lands near gold-dark's luminance band — and the pass-2 fix explicitly set hex-distinctness as the bar, which is met. Both tokens always carry non-color pairings (§6.5: node icon/ring; ring labels), and their primary contexts differ (skill tree vs. nutrition rings / gold UI components). **Non-blocking**, with one carry-forward for screens noted below (Combined Progress Dashboard is the one screen where both could co-occur).
- The darker "muted gold" still suits §5.2's calm register: a dark, thin, low-visual-weight bronze fill on a warm light surface reads quiet, exactly the "reduce weight, not contrast" resolution pass 2 suggested. (Naming nit: `gold-muted` is now darker than `gold-dark`, which is mildly counterintuitive as a token name — cosmetic, non-blocking.)

### The two additive notes (question 4)

- **§1.4 locked-state exemption:** reasonable and correctly reasoned — the WCAG 1.4.11 exception for inactive UI components is real, locked's base hex genuinely passes in isolation (recomputed ~3.3:1 on `#F7F3EE`), and the note honestly identifies the 60%-opacity rendering as the thing being exempted rather than hiding it. Critically, it states its own invalidation condition (locked nodes becoming tappable). One tension to carry forward rather than reject on: the approved sitemap's nav line "Tier/Node Map → Node Detail" and the research's "tapping a node opens a detail/technique view" don't specify whether *locked* nodes are tappable. The sitemap is not this stage's dependency, and the sitemap's Tier/Node Map shows prerequisites on the map itself (so a non-tappable locked node is a coherent reading), but the screens stage must resolve this explicitly — see carry-forwards.
- **§4.7 outline clarification:** correct and consistent — the boss/milestone outline now names `color.node.unlocked` (`#A85F12`) with an explicit warning off `color.primary.gold`, matching §1.4's mastered-state spec exactly. This was pass 2's optional fix 3 (first half), done as suggested.

### Preservation check (question 3 — against the pass-2 recorded state and the pass-1 approved-as-is list)

- **§0:** intact (Option 2 restrained, tokenism reasoning, perf convergence, revisitability, photography consequence).
- **§1.1–§1.3, §1.5:** intact except the two required token-row updates (gold-muted rewritten per fix 1; everything else verbatim in substance, including the secondary-button single-owner note and the gold-dark row).
- **§1.4:** locked/in-progress/completed/mastered hexes unchanged from pass 2 (`#8A8578`, `#3B7EA8`, `#3D8B5C`, `#A8452A`); only the unlocked hex, the consistent mastered-outline reference, and the instructed exemption paragraph changed/added.
- **§2, §3:** intact (system font, timer-xl 64pt + clamp disclosure; 4px base, 44pt no-exceptions, density modes).
- **§4.1–§4.6, §4.8–§4.10:** intact, including the barcode deferral (§4.3), the portion-photo component (§4.4), the rest-timer state (§4.8/§5.1 with lock-screen carry-forward), and §4.10's honest reuse rationale — whose "distinct from §1.4" claim remains true against the new hexes (re-verified).
- **§5:** two-preset structure intact; §5.1 unchanged including the node-colors-out-of-live-view guard; §5.2 changed only in the required accent line.
- **§6 rules 1–9:** intact; §6.10's base text intact with the audited-pairings additions. Known non-blocking carryover: §6.4 still cites WCAG 2.5.4 for no-flashing (should be 2.3.1) — flagged as non-blocking in pass 2 and unchanged; the build stage should not copy that citation into compliance docs.
- Closing statement present.

Nothing removed or thinned; every change traces to the pass-2 fix list. All prose claims checked against the tables and against independent recomputation — no false internal assertions remain.

### Non-blocking observations (recorded, not rejection grounds)

1. Pass 2 verified the four unchanged node hexes against light surfaces only. Spot-checking the dark side for completeness: on `dark-bg #17181A` in-progress ≈ 4.0:1, completed ≈ 4.3:1, unlocked ≈ 3.65:1 all pass; **mastered `#A8452A` is marginal at ≈ 3.0:1 on `dark-bg` and ≈ 2.6:1 on `dark-surface #232427`**. This hex was pass-2-approved and preserve-listed, mastered nodes carry a star/badge icon plus an outline in `#A85F12` (which itself passes at ≈ 3.19:1 on dark-surface), so a compliant indicator exists — but the screens stage should keep dark-mode node maps on `dark-bg` (not card surfaces) or lean on the outline/icon as the §6.10-bearing indicator for mastered in dark mode.
2. §6.4's 2.5.4-vs-2.3.1 citation (above).
3. Token naming: `gold-muted` is now the darkest gold variant (cosmetic).

**Verdict: APPROVED.** Both dependencies of the screens stage (`sitemap`, `design-system`) are now approved — the screens stage is fully unblocked. Carry-forwards for the screens stage, in addition to the three recorded in the sitemap approval: (4) use only `#A85F12` for unlocked/boss-outline and `#7A4E12` for the Pilates progress fill — the superseded drafts `#C98A2E` and `#E0BE7C` must not reappear; (5) decide explicitly whether locked nodes are tappable (sitemap's Tier/Node Map → Node Detail path vs. §1.4's non-interactive premise) — if tappable, §1.4's inactive-component exemption is void by its own terms and the locked state needs a ≥3:1 treatment; (6) on the Combined Progress Dashboard, don't place the carbs ring (`gold-dark #A9761E`) and unlocked-node indicators (`#A85F12`) adjacent without their icon/label pairings — they are hex-distinct but perceptually close; (7) in dark mode, render node maps on `dark-bg`, using the mastered outline/icon as the contrast-bearing indicator (observation 1). Reviewer note per this stage's known difficulty: the upcoming screens review is the pipeline's merge point (sitemap + design-system) — the orchestrator should consider running it at effort `xhigh`.

## 2026-07-10 — Stage: screens (MERGE POINT: sitemap + design-system) — Verdict: APPROVED

**Reviewed output:** `docs/screens.md`
**Dependencies (both checked independently):** `sitemap` (approved, pass 2) → `docs/sitemap.md`; `design-system` (approved, pass 3) → `docs/design-system.md`. This is the fork-join merge point; each dependency was checked on its own terms, and the seven design-system carry-forwards plus the three sitemap carry-forwards were verified one by one.

### Check 1 — Against the sitemap: full screen coverage + traceability structure

Every screen in the sitemap's screen list maps to a real section in `docs/screens.md`, verified one-to-one:

- **Onboarding (A):** Splash (A1), Sign Up/Log In + forgot-password (A2), Profile Setup (A3), Goal & Target Setup (A4), Region & Cuisine (A5), Module Interest (A6), Workout Placement Assessment (A7 with **all five sub-screens**: Assessment Intro A7a, Track Selection A7b, Calisthenics Steps A7c, Calisthenics Results A7d, Pilates Steps A7e, Pilates Results A7f, Combined Assessment Summary A7g), Onboarding Complete (A8).
- **Home:** present with all four sitemap elements (energy-balance card, micronutrient snapshot with "no data" state, quick actions Log Meal / Log Workout Session, sync/offline indicator).
- **Nutrition (N1–N13):** Food Diary, Add Entry, Food Search Results, Ingredient Detail, Composite Meal Detail, Portion Reference Guide, Custom Food/Meal Builder, Confirm & Log, Edit/Delete Entry, Daily Nutrition Summary, Micronutrient Detail, Nutrition History/Calendar, Favorites & Recents — all present.
- **Workout (W1–W8):** Skill Tree Home, Tier/Node Map, Node Detail, Log Attempt, Mastery Gate Confirmation, Progression Status, Workout Session Log, Workout History/Session Calendar — all present.
- **Progress (P1–P3):** Combined Progress Dashboard, Weight Log, Goal Settings — present.
- **Profile/Settings (S1–S8):** Profile, Account, Data & Sync, Integrations, Region & Language, Notifications, Legal & Disclaimers, Help/Support — all present.
- **Modals:** all six sitemap modals mapped (Add Food Entry→N2, Edit/Delete→N9, Portion Reference Guide→N6, Log Attempt→W4, Mastery Gate→W5, Quick-add Weight→P2).

**Per-track placement flow (sitemap carry-forward 3 + Req 9): fully represented.** A7b Track Selection appears only when both tracks are opted into; A7c/d Calisthenics and A7e/f Pilates each have their own steps→results, in either order; A7g Combined Summary appears only on the both-tracks path; deferral is handled at A7a ("do this later") and A7b, with re-entry from **Skill Tree Home (W1)** via a per-track "Complete placement" CTA. All one/other/both/deferred states covered.

**Portion-photo reference (sitemap carry-forward 2 + Req 1): present on all three required surfaces** as a real, described §4.4 element — Ingredient Detail (N4, inline tile alongside the picker), Composite Meal Detail (N5, same pattern), and the Portion Reference Guide (N6, its dedicated expanded-set home). Offline/omitted-when-absent fallbacks are described, not hand-waved.

Each screen delivers what the sitemap said it needed (spot-checked: N4 has the household-unit-first picker with grams demoted and no barcode; N10 has the full micronutrient panel with per-nutrient "no data"; W3 states threshold as TBD with form-cue content; P1 delivers the cross-module weight/calorie/tier payoff).

### Check 2 — Against the design-system: components/tokens/rules compliance

- **Approved hexes only.** Unlocked node fill and boss/mastered outline use `color.node.unlocked #A85F12` (W2, W3, W5, P1); Pilates progress indicator uses `gold-muted #7A4E12` (W7 flag / §5.2 reference). The superseded drafts `#C98A2E` and `#E0BE7C` appear **only** in the doc's own "never used" disclaimers (how-to-read note + carry-forward 4) — never as an actual assignment. Confirmed (design-system carry-forward 4).
- **Locked nodes fully non-interactive.** Carry-forward 5 and W2/W3 document locked nodes as non-tappable "not-yet-available" indicators that open nothing; Node Detail (W3) is reachable **only** for unlocked/in-progress/completed/mastered nodes; unlock requirements are shown as a static, non-interactive caption near the locked node and on the unlocked/in-progress detail. This is the reading that **keeps** the §1.4 / §6.10 WCAG 1.4.11 inactive-component exemption valid (had locked nodes been made tappable, the exemption would be void by its own terms). It does not contradict the sitemap, which never asserts locked-node tappability and shows prerequisites on the map itself. Correct resolution of design-system carry-forward 5.
- **Combined Progress Dashboard adjacency (carry-forward 6).** P1 places the carbs macro-ring (`gold-dark #A9761E`) and unlocked-node indicator (`#A85F12`) in **distinct titled sections** (nutrition vs. workout), and each **always** carries its required §6.5 icon/label pairing; neither is ever a bare colored dot next to the other. Correct.
- **Dark-mode node maps (carry-forward 7).** W1/W2/W3 render on `color.neutral.dark-bg #17181A` (not `dark-surface`) in dark mode, and the mastered node's **outline `#A85F12` + star/badge icon** — not its `#A8452A` fill — carry the primary contrast signal, matching the design-system's own non-blocking observation that mastered fill is only marginally compliant on dark surfaces. Correct.
- **No invented components or colors.** Every color referenced maps to a defined token; every composed control maps to a §4 component, or is explicitly flagged as a gap (below).

### Check 3 — Honesty on flagged gaps (verified accurate, not invented-around)

The five flagged missing components are each genuinely absent from design-system §4 and genuinely required by a sitemap screen: (1) **segmented control / in-sheet tab bar** — A2 mode switch, N2 Search/Recent/Favorites/Custom tabs, W1 track selector (§4.6 is the bottom nav tab bar only); (2) **single-select form control** — A3/A5/N8/N9/P3/S1/S5 and the A7c/e self-report (§4.3 chips are scoped to portion units); (3) **toggle/switch** — A5 food-set enable, S3 low-data, S6 notifications; (4) **calendar/date-picker** — N12, W8; (5) **line/trend chart** — N11, P1, P2 (§4.10 is rings/bars only). All confirmed genuine. Interim reuse (chip pattern as stopgap single-select) is disclosed as a stopgap, not passed off as a match. A sixth item (formal list-row) is honestly noted as low-risk layering on §4.5.

**Session-player scope mismatch (W7 + Consolidated section): accurate and honestly handled.** The sitemap's Workout module is scoped to freeform logging (Workout Session Log W7 + Log Attempt W4) with **no** guided/timed session-player destination, while design-system §4.8/§5 fully specs a shared player (calisthenics rest-timer preset §5.1, Pilates audio-led `gold-muted` preset §5.2). Verified: W7 is genuinely freeform logging (attempt rows + running summary) — **no player screen, rest-timer sequence, or audio-led class UI was invented** to paper over the gap. The doc explicitly states "I did not silently create a player screen" and preserves the tokens/behaviors so the spec isn't lost. This is the correct handling of a genuine upstream inconsistency between two approved docs.

### Check 4 — Discipline on unvalidated content (Open Question 1): preserved

No invented calisthenics/Pilates exercise names, real progression thresholds, or asserted node ordering anywhere. The placement steps stay at the sitemap's abstraction level (A7c push/pull/squat-hinge/core-hold "generic, content TBD"; A7e core/breath/mobility/mat-position "generic, content TBD"); results are generic "start at Tier N", no named skills; W2/W3/W6 use `[Skill Node]` placeholders and show unlock requirements as `TBD`; prerequisites are listed generically without asserting a validated order. The sitemap's placeholder discipline (reflecting product-research Open Question 1) is intact.

### Reviewer's adjudication of the session-player mismatch (for the build stage)

The mismatch is real but is not a screens-stage defect — screens flagged it correctly rather than inventing. Recommended resolution: **option (a)** — the §4.8/§5 session-player component is intentionally ahead of the v1 sitemap. The sitemap deliberately scopes Workout to freeform logging, so **the build stage should build W7 freeform logging only and must NOT build a guided Session Player screen** (calisthenics rest-timer sequence or Pilates class player) that has no sitemap destination. The player component/tokens stay deferred until a future sitemap revision adds a guided-session destination. This keeps build scoped to the approved sitemap.

### Non-blocking observations (recorded, not rejection grounds)

1. **Expandable/accordion interaction** is used in A4 ("Adjust targets" expandable area), N5 (recipe-breakdown expandable), and S8 (FAQ expandable rows) without a formal §4 component and without a gap flag — comparable to the list-row case the doc *did* flag (Consolidated gap 6). Low-risk (expand/collapse layered on §4.5 Cards, introduces no new color/rule), but for completeness the design-system owner could fold it into the same additive list.
2. **A7g Combined Assessment Summary** names the calisthenics accent (`deepgreen`) but does not explicitly state the Pilates card's accent; both tracks are workout-mode so `deepgreen` applies — a one-line clarification, cosmetic.
3. **A1 Splash** uses `type.display` for the wordmark, slightly outside its stated "session-complete/celebratory" use; it is only a size/weight token and a splash wordmark is a defensible singular use — non-blocking.
4. Informal icon-buttons (A2 password show/hide, trailing edit/remove icons) rely on the 44pt hit-area rule but aren't a named §4 component — trivial, treated as text/tertiary affordances.

### Preservation / no-invention posture

The document did not thin either dependency: it composes strictly from defined tokens/components, and where the sitemap requires something undefined it flags rather than invents. The prose does not over-claim — every "carry-forward resolved" statement was checked against the actual section content and holds.

**Verdict: APPROVED.** Both dependencies are satisfied independently; all seven design-system carry-forwards and all three sitemap carry-forwards are addressed; the five component gaps and the session-player scope mismatch are honestly flagged (not invented around); Open Question 1 placeholder discipline is intact. Build stage is unblocked. **Build-stage carry-forwards:** (1) build W7 freeform logging only — do NOT build a guided Session Player screen; the §4.8/§5 player component is deferred until a sitemap destination exists; (2) implement the five flagged components (segmented control, single-select, toggle/switch, calendar/date-picker, trend chart) as additive to the design system, replacing the interim chip-as-single-select stopgap; (3) do not reintroduce `#C98A2E`/`#E0BE7C`; keep node maps on `dark-bg` in dark mode with mastered outline/icon as the contrast-bearing signal; keep locked nodes non-interactive (or the WCAG 1.4.11 exemption must be re-verified); (4) do not copy the §6.4 WCAG 2.5.4-for-no-flashing citation into compliance docs (should be 2.3.1).

## 2026-07-10 — Stage: build (FINAL STAGE) — Verdict: REJECTED

**Reviewed output:** `app/` — the entire `app/src` tree was read file-by-file (App.tsx, 6 navigators, 20 components, 43 screens, theme/state/data/utils), plus `app/BUILD_NOTES.md`.
**Dependencies:** `screens` (approved) → `docs/screens.md`; cross-checked against `docs/sitemap.md` (hierarchy), `docs/design-system.md` (tokens/rules), and the four build carry-forwards recorded in the screens approval.

### What passes (verified in source, not taken from BUILD_NOTES)

- **Navigation structure vs. sitemap/screens: PASS.** `RootNavigator` implements Splash → Auth → Onboarding → five-tab shell exactly per the sitemap's pre-nav sequence. `MainTabs` has all five tabs (Home, Nutrition, Workout, Progress, Profile) with terracotta active state + underline indicator (not color alone) and a sync-error tab badge. Onboarding covers A1–A8 in order, with the A7 per-track placement flow fully implemented: Assessment Intro (per-track statement, safety callout, skip/defer), Track Selection (either order, deferral, both-tracks Continue gating), shared per-track Steps/Results screens with genuinely distinct step categories per track, conditional Combined Summary, and deferred-placement re-entry from Skill Tree Home via `returnTo`/`forceTrack` params re-registered in `WorkoutNavigator`. Nutrition N1–N13, Workout W1–W8, Progress P1–P3, and Profile S1–S8 all have real routes/screens. Two consolidations (N3 rendered inline in N2 via `FoodResultsList`; N4/N5 merged into one `FoodDetailScreen` branching on `food.kind`) are consistent with the sitemap's nesting and are honestly documented in BUILD_NOTES.
- **Tokens vs. design-system: PASS.** `src/theme/tokens.ts` transcribes the approved palette 1:1 — brand, semantic, neutrals, and all five dedicated node hexes verified against the design-system tables, including `color.node.unlocked #A85F12` and `color.primary.goldMuted #7A4E12`. Type scale (base 16 / 1.25 ratio, timer-xl 64), 4px spacing, 44pt touch-target constant, and density presets all match. **The superseded `#C98A2E` and `#E0BE7C` appear nowhere in `app/src`** — verified by exhaustive read of every source file (no shell/grep was available to this review session, so the check was done by reading the full tree).
- **Placeholder discipline (Open Question 1): PASS.** `src/data/skillTree.ts` is mechanism-only: every node is "Skill Node A/B/..." with `thresholdLabel: 'TBD'`, generic placeholder form cues, and a prominent do-not-treat-as-real header. Placement steps are generic category checks; results are generic "Tier N"; Node Detail shows the threshold as TBD with the carry-forward cited; no real exercise names, orderings, or thresholds anywhere in the UI. `src/data/foodDatabase.ts` is prominently labeled mock/placeholder (not sourced FAO/INFOODS/WAFCT/Kenya-FCT data, must be replaced), `MICRONUTRIENT_TARGETS` is flagged as invented, and the Micronutrient Detail screen carries an in-app placeholder-data caveat.
- **No guided Session Player (carry-forward 1): PASS.** `WorkoutNavigator` has no player route; `src/screens/workout/` contains exactly W1–W8; `WorkoutSessionLogScreen` is genuinely freeform (pick actionable node → Log Attempt → session summary), with the deferral documented in its own header comment. No rest-timer sequence or audio-led player exists anywhere.
- **Locked nodes non-interactive (carry-forward 3 / screens carry-forward 5): PASS.** `SkillNode.tsx` hard-guards it regardless of props: `onPress={locked ? undefined : onPress}`, `disabled={locked}`, `accessibilityState={{ disabled: locked }}`, no `button` role when locked. W2 routes to Node Detail only via the node's own (guarded) press.
- **Dark-mode node maps (carry-forward 3): PASS.** W1/W2/W3/W4/W5 force dark with background `#17181A` (dark-bg, not dark-surface); mastered nodes render the `#A85F12` outline + star icon as the contrast signal.
- **Additive components (carry-forward 2): PASS.** All five flagged gaps built as real components consistent with existing tokens (SegmentedControl, SingleSelect via a shared Chip primitive, ToggleSwitch, Calendar with icon+color completeness indicators, TrendChart with genuine null-gap rendering — no false zeros), plus the formalized ListRow. `PortionUnitPicker`/`PortionPhotoReference` implement §4.3/§4.4 faithfully (household-unit default, grams demoted, no barcode, tile omitted when no reference, offline icon+caption fallback).
- **Offline-first (Req 5): PASS as a demo.** Real NetInfo + a documented "Simulate offline" toggle; queued flags on diary/weight/attempt/session entries; queue count surfaced on Home and Data & Sync; manual Sync now; auto-drain on reconnect. The no-real-backend limitation (nothing ever sets `syncFailed`) is honestly disclosed in BUILD_NOTES.
- **BUILD_NOTES honesty: mostly verified true.** Spot-checked claims all match the code: `advanceNodeState()` behaves exactly as described (one state per attempt, boss→mastered, downstream unlock with all-prerequisites check); `WORKOUT_MODULE_LIVE = true` with the disabled/"Coming soon" path genuinely present in `ModuleInterestScreen`; the 1.5s mock sync drain; no gesture-handler dependency in `BottomSheet`; Mifflin-St Jeor with constant age 30 in `calculateGoals.ts` (age genuinely isn't a collected field); N7's micronutrient/photo-attach cut disclosed both in BUILD_NOTES and in the screen's own copy; S7 legal copy flagged as placeholder in-app. **One claim is false — gap 1 below.**

### Concrete gaps (reasons for rejection)

1. **`gold-muted #7A4E12` is used as a decorative fill, and BUILD_NOTES falsely claims it is unused.** BUILD_NOTES deviation #3 states goldMuted is "reserved-but-unused since no Pilates player exists." False: `src/data/foodDatabase.ts` assigns `tone: '#7A4E12'` to the Waakye portion-photo placeholder tile, and `PortionPhotoReference.tsx` renders `tone` as the tile's flat `backgroundColor` — a decorative background fill. This violates the design system's hard-won §1.1 restriction (progress-fill role only, "never a background/decorative fill" — a rule `tokens.ts` itself repeats on the token's own line) and is a false internal claim of exactly the kind this pipeline has rejected twice before (research-design pass 2, design-system pass 1). Compounding: other tile tones reuse **semantic** hexes as decoration — `#2E7D46` (semantic.success) on Sukuma Wiki and `#B8752A` (semantic.warning) on Ugali — though §1.3 semantic colors carry "fixed meaning app-wide."
2. **Node-state chips render node states in semantic colors instead of the §1.4 node vocabulary.** `NodeDetailScreen` (W3) and `ProgressionStatusScreen` (W6) map node states to `StatusBadge` semantic tones: unlocked/in-progress → `info` (`#2A6F97`), completed → `success` (`#2E7D46`), **mastered → `warning` (`#B8752A`)**. `docs/screens.md` W3 explicitly requires "a state chip using the §1.4 node color + icon/label for its current state." Rendering "Mastered" in the app's warning orange both contradicts the screens spec and re-introduces the ad-hoc node-state/semantic color cross-contamination the design-system stage took three passes to eliminate. (A `STATE_TONE` copy in `CombinedProgressDashboardScreen` is dead code — also cleanup.) Undocumented in BUILD_NOTES.
3. **Custom foods (N7 / Req 2) are a save-only dead end.** `CustomFoodBuilderScreen` dispatches `ADD_CUSTOM_FOOD` into `state.customFoods` — and nothing in the app ever reads `state.customFoods`. Add Entry's Search/Recent/Favorites pools draw only from `FOOD_DATABASE` (`searchFoods`/`getFoodById`), so a created custom food can never be found, previewed, or logged. The screens spec's "Save & log now (→ Confirm & Log)" action is also not implemented — the button just saves and `goBack()`s without logging anything. Req 2's "user creates and saves an ingredient or composite meal" is therefore non-functional end-to-end, and this cut is not documented (BUILD_NOTES discloses only the micronutrient/photo-attach omissions for N7, implying the rest works).
4. **"Adjust a placement" silently corrupts placement state.** `CombinedSummaryScreen`'s "Adjust a placement" navigates to `PlacementResult` with fabricated params (`{ track: 'calisthenics', yesCount: 1, totalSteps: 1 }`); `PlacementResultScreen`'s mount effect then dispatches `SET_PLACEMENT`, overwriting the user's real calisthenics starting tier to Tier 3 (`1 + floor(1/1 × 2)`). The screens spec (A7g) says this action "routes back to the relevant track's result" — it must not fabricate a new result, and it hardcodes calisthenics regardless of which placement the user wants to adjust.
5. **(Minor) P1 violates carry-forward #6's letter on both named indicators.** `docs/screens.md` P1 states the carbs indicator and unlocked-node indicator each "always carries its required icon/label pairing" and "Neither is ever shown as a bare colored dot." `CombinedProgressDashboardScreen` renders both as bare 12px colored `View` dots with a text caption — text labels and separate titled sections are present (the disambiguation intent survives), but the §1.4 outlined-circle icon pairing for the unlocked indicator is missing and both are literally the bare-dot rendering the spec forbids.
6. **(Minor, undocumented deviation) W2 Tier/Node Map shows no prerequisite information.** The screens spec requires prerequisite edges "drawn as plain lines connecting nodes" and a static non-interactive unlock caption near locked nodes; the sitemap (Req 8) says each node shows "its prerequisite node(s)." `TierNodeMapScreen` renders tier rows of nodes with no edges and no captions — prerequisites appear only on Node Detail (W3). Possibly an acceptable build simplification, but it is a silent deviation in a file whose BUILD_NOTES claims "nothing was silently improvised."
7. **(Minor) Weight Log deletes on a bare row tap with no confirm step.** `WeightLogScreen` wires `onPress` of each entry row directly to `DELETE_WEIGHT`. Destructive actions elsewhere (N9 entry delete, S2 account delete) correctly use an explicit confirm state; this one is a single accidental tap from data loss.
8. **(Minor, latent bug) `FoodDetailScreen` violates the Rules of Hooks.** The `if (!food) return ...` early return sits above a `useMemo` call, so the hook count differs between the found/not-found branches. Currently unreachable in practice (all navigations pass valid database ids), but it will crash React the moment gap 3 is fixed by routing custom-food ids here.

### Verification limitation (recorded honestly)

The builder's "Verified" section claims `npx tsc --noEmit` passes with zero errors and `npx expo export --platform android` bundles cleanly. **This review session had no shell tool available (file read/write only), so neither claim could be independently re-run.** The full-tree read found no type errors by inspection (and the dead `STATE_TONE` in P1 indicates `noUnusedLocals` is off, consistent with a passing tsc), but per this pipeline's trust-nothing standard the next pass should include the actual tsc output (or the orchestrator should re-run it) so the claim is verified rather than trusted.

### Required changes for approval (narrow — everything in "What passes" must be preserved as-is)

1. Remove `#7A4E12` from the Waakye tile tone (use a non-reserved brand/neutral hue) and replace the semantic-hex tile tones (`#2E7D46`, `#B8752A`) with brand/neutral values; correct BUILD_NOTES deviation #3 so the goldMuted sentence is true of the code (the token may remain defined-but-unused, or its single legitimate absence explained).
2. Render node-state chips with the §1.4 node colors + state icon per screens W3 (a small node-state badge variant is fine); "mastered" must never render in `semantic.warning`. Remove the dead `STATE_TONE` copy in `CombinedProgressDashboardScreen`.
3. Make custom foods functional or document the cut: include `state.customFoods` in Add Entry's Custom tab (and ideally search), make them loggable through Confirm & Log, and implement "Save & log now" → Confirm & Log per the screens spec — or, at minimum, add an explicit BUILD_NOTES deviation stating custom foods are create-only in this build.
4. Fix `CombinedSummaryScreen`'s "Adjust a placement": route to the chosen track's existing result (or re-run its steps) without dispatching a fabricated placement — e.g., a review-only param that suppresses `PlacementResult`'s `SET_PLACEMENT` dispatch, and let the user pick which track to adjust.
5. Add the icon pairings to P1's two indicators (outlined-circle glyph in `#A85F12` for unlocked; an icon/ring treatment for the carbs metric) so neither is a bare colored dot, per carry-forward #6.
6. Add the static prerequisite caption near locked nodes (and/or simple edge lines) on `TierNodeMapScreen`, or document the omission as an explicit BUILD_NOTES deviation.
7. Add a confirm step to Weight Log entry deletion (match the N9 pattern).
8. Move `FoodDetailScreen`'s not-found early return below the hooks (or fold the memo logic so hook order is unconditional).
9. Include fresh `npx tsc --noEmit` (and ideally `expo export`) output with the revision so the verification claims can be checked rather than trusted.

### Non-blocking observations (recorded, not rejection grounds)

1. Raw hex literals outside `tokens.ts` despite that file's own "do NOT introduce new hex values outside this file" rule: most are verbatim copies of token values (`#17181A` in ScreenContainer, `#F7F3EE` in Input, `#C9C6BE` dark-secondary text in workout screens) — hygiene, import the tokens; `StatusBadge`'s tint backgrounds and `Card`'s pressed-state hexes are genuinely new values, plausibly the §4.9 "10% tint" treatments, and should eventually be tokenized.
2. `FavoritesRecentsScreen`'s favorite star uses `color.primary.gold`, whose §1.1 scope is streak/XP-adjacent accents — arguably adjacent, worth a one-line justification.
3. `ThemeContext` supports dark mode with no exposed toggle — already honestly documented in BUILD_NOTES; fine.
4. `AccountSettingsScreen` "Save changes" is a no-op (`onPress={() => {}}`) — acceptable for a no-backend build but worth an inline comment.
5. Session-note `Input` on W7 and the FAQ accordion on S8 mirror the screens doc's own non-blocking accordion observation — fine.

**Verdict: REJECTED** — narrowly, against a build that is otherwise the strongest first-pass artifact this pipeline has produced (structure, placeholder discipline, token fidelity, and offline behavior all verified faithful). The rejection rests on one false BUILD_NOTES claim paired with a reserved-token misuse (the pipeline's twice-rejected failure pattern), one direct screens-spec violation on node-state colors, one non-functional requirement path (custom foods), and one state-corrupting bug — all mechanical, narrowly-scoped fixes. Everything listed under "What passes" is explicitly approved as-is and must not be thinned while fixing.

## 2026-07-10 — Stage: build (FINAL STAGE, SECOND PASS) — Verdict: APPROVED

**Reviewed output:** `app/` (revision after first-pass rejection), checked against the nine-item fix list above by reading every file the fixes touch: `src/data/foodDatabase.ts`, the new `src/components/NodeStateBadge.tsx`, `src/components/SkillNode.tsx`, `src/screens/workout/NodeDetailScreen.tsx`, `ProgressionStatusScreen.tsx`, `TierNodeMapScreen.tsx`, `src/screens/progress/CombinedProgressDashboardScreen.tsx`, `WeightLogScreen.tsx`, `src/screens/nutrition/AddEntryScreen.tsx`, `FoodDetailScreen.tsx`, `ConfirmLogScreen.tsx`, `CustomFoodBuilderScreen.tsx`, `FoodResultsList.tsx`, `FavoritesRecentsScreen.tsx`, `PortionReferenceGuideScreen.tsx`, `src/components/PortionPhotoReference.tsx`, `src/screens/placement/CombinedSummaryScreen.tsx`, `PlacementResultScreen.tsx`, `PlacementStepsScreen.tsx`, the three navigators (`OnboardingNavigator`, `WorkoutNavigator`, `NutritionNavigator`), `src/theme/tokens.ts`, `src/state/types.ts`, `src/state/AppStateContext.tsx`, `src/data/skillTree.ts`, and `app/BUILD_NOTES.md`.
**Dependencies:** `screens` (approved) → `docs/screens.md`; cross-checked against `docs/design-system.md` token rules and the four build carry-forwards.

### First-pass fix list: all nine items verified DONE in source (not taken from BUILD_NOTES)

| Required change (pass 1) | Status in this revision |
|---|---|
| 1. Remove `#7A4E12` and semantic hexes from tile tones; correct the false BUILD_NOTES claim | **Done.** `foodDatabase.ts` tile tones are now exclusively §1.1 brand hues: `#A9761E` gold-dark (Jollof, Banku, Oats), `#C1502E` terracotta (Jollof-2, Fufu), `#1F5C42` deepgreen (Egusi, Sukuma Wiki), `#D9A441` gold (Waakye), `#96391F` terracotta-dark (Ugali, Nyama Choma). `#7A4E12`, `#2E7D46` (success), and `#B8752A` (warning) appear in none of the tone assignments. `PortionPhotoReference.tsx` now documents the token restriction on the `tone` field itself (never goldMuted, never `semantic.*`). BUILD_NOTES deviation #3 is rewritten to the true statement — goldMuted is *now* genuinely unused (verified: its only occurrence is its definition in `tokens.ts`, whose own inline comment carries the restriction), and the note explicitly acknowledges the prior claim was false rather than papering over it. Decorative use of base gold `#D9A441` is permissible — §6.10 bans it only for stateful/interactive components, and a placeholder photo tile is neither. |
| 2. §1.4 node-state chips + dead `STATE_TONE` removal | **Done.** New `NodeStateBadge.tsx` renders border + icon + label all from `NODE_STATE_META`, which is now exported from `SkillNode.tsx` — the exact same color/icon/label mapping the tree map itself uses, so map and chips cannot drift. `NodeDetailScreen` (W3) and `ProgressionStatusScreen` (W6) both use it; neither imports `StatusBadge` anymore; "Mastered" renders in `color.node.mastered #A8452A` with a star icon, never `semantic.warning`. The dead `STATE_TONE` copy is gone from `CombinedProgressDashboardScreen`. `StatusBadge` survives only for genuine app-wide semantic meanings (offline info, "Saved" success) — correct division. |
| 3. Custom foods functional end-to-end | **Done, traced end-to-end.** `foodDatabase.ts` gains `customFoodToFoodItem` (projects a `CustomFoodEntry` into the `FoodItem` shape with a `'Custom'` region, the user's single declared unit, empty micros/photoRefs — each omission consistent with N7's documented scope), `searchAllFoods`, and `findFood` (seed-first, then custom). `AddEntryScreen` merges `state.customFoods` into **all four** tabs: Search (both empty-query region-filtered list and query results), Recent and Favorites (id resolution falls back to custom items), and the Custom tab lists them under the "Build a custom food or meal" CTA with a correct empty state. `FoodDetailScreen` and `ConfirmLogScreen` both resolve `foodId` via `findFood`, so a custom food opens in Food Detail and logs through Confirm & Log; `ADD_RECENT` with a custom id round-trips correctly back into Add Entry's Recent tab. `CustomFoodBuilderScreen`'s "Save & log now" now dispatches `ADD_CUSTOM_FOOD` **and** navigates to `ConfirmLog` with a real 1×-declared-unit portion (same param shape Food Detail sends), instead of `goBack()`. Reducer/persistence for `customFoods` verified in `AppStateContext.tsx`/`types.ts`. The in-screen claim ("searchable/loggable from Add Entry's tabs and Food Detail") is true as scoped. |
| 4. "Adjust a placement" state corruption | **Done, dispatch path traced.** `CombinedSummaryScreen` no longer fabricates params: it offers two explicit buttons ("Adjust calisthenics placement" / "Adjust Pilates placement"), each navigating with `{ track, returnTo, review: true }` — the user picks the track; nothing is hardcoded. `PlacementResultScreen` in review mode: `computedTier` is not even computed (`!review` guard, so the absent `yesCount`/`totalSteps` are never touched), the mount `useEffect` early-returns **before** the `SET_PLACEMENT` dispatch, and the displayed tier is read from `state.placement[track].startingTier` — the real stored value. The only path that changes stored placement is the explicit "Retake" → `PlacementSteps` → a **fresh** non-review `PlacementResult`; stack-order check confirms the prior `PlacementResult` instance is popped when Retake navigates back to `PlacementSteps`, so the post-retake result is a new mount whose effect legitimately re-dispatches. Continue in review mode returns to `CombinedSummary` preserving `returnTo`. No silent overwrite remains on any traced path. |
| 5. P1 icon pairings | **Done.** The unlocked-node indicator is now an `ellipse-outline` Ionicons glyph in `color.node.unlocked` (matching §1.4's unlocked icon exactly as `NODE_STATE_META` defines it) and the carbs metric a `pie-chart-outline` glyph in `macroColor.carbs` — no bare 12px `View` dots remain; the separate titled sections and text labels are preserved. |
| 6. W2 prerequisite info | **Done via the caption option.** Each locked node on `TierNodeMapScreen` renders a static, non-Pressable "Requires: [names]" caption from `getNodeDef`, width-aligned under the node. Edge lines were not drawn — the fix list's "caption (and/or simple edge lines)" wording explicitly permits caption-only, and BUILD_NOTES now documents the choice rather than leaving it silent. Locked-node non-interactivity and forceDark are untouched. |
| 7. Weight Log delete confirm | **Done.** Row tap now only sets `confirmDeleteId`; the row swaps to an error-state Card with explicit "Yes, delete" / "Cancel" actions (matching the N9/S2 pattern), plus a caption telling the user a confirm step follows. `DELETE_WEIGHT` dispatches only from the confirm button. |
| 8. FoodDetailScreen Rules of Hooks | **Done.** All hooks (`useMemo` for `findFood`, `useState` ×5, `useMemo` for scaled micros) now run unconditionally; the `if (!food)` early return sits below the last hook, and the intermediate derivations are null-guarded. Hook count is identical across found/not-found branches; the fix-3 custom-food routing that would have triggered the old crash is therefore safe. |
| 9. Fresh tsc/export output | **Verified via the orchestrator, disclosed honestly.** This review session again has no shell tool (file read/write only), so the commands could not be re-run by the reviewer directly. However, the orchestrator independently ran `npx tsc --noEmit` and confirmed exit code 0 both before and after this revision — the fix list's stated fallback ("or the orchestrator should re-run it") is satisfied. BUILD_NOTES' Verified section now states the output was included in the revision response rather than only claimed, consistent with that run. |

### Preservation check (against the pass-1 "What passes" list)

Every file touched by the fixes was re-read; nothing was thinned. Navigation structure intact (`OnboardingNavigator` A3–A8 + A7 flow, `WorkoutNavigator` W1–W8 + placement re-registration with **no player route**, `NutritionNavigator` N1–N13 with the documented consolidations). `tokens.ts` still transcribes the palette 1:1; `#C98A2E`/`#E0BE7C` still appear nowhere (header comment reconfirms; none of the revised files introduce them). `SkillNode.tsx`'s locked-node hard guard (`onPress={locked ? undefined : onPress}`, `disabled`, no button role) is unchanged — the only change is the additive `NODE_STATE_META` export. `skillTree.ts` placeholder discipline intact (generic "Skill Node A–F", `thresholdLabel: 'TBD'`, placeholder form cues, mechanism-only header). `foodDatabase.ts` mock/placeholder labeling intact, "no data" micronutrient semantics intact. Offline-first machinery (`queued` flags, NetInfo + simulate-offline, 1.5s drain, queue counts) untouched. W2/W3 still force dark with the mastered outline/icon signal. BUILD_NOTES round-2 section accurately describes every change — each claim in it was checked against the code above and holds; the honesty failure from pass 1 is repaired, including an explicit acknowledgment of the prior false claim.

### Non-blocking observations (recorded, not rejection grounds — candidates for a cleanup pass, none violates a dependency requirement or a BUILD_NOTES claim)

1. **`PortionReferenceGuideScreen` (N6) still resolves via `getFoodById` only.** A custom food's Food Detail renders the always-present "View portion guide" button; tapping it for a custom id returns `null` — a blank modal (dismissable via native modal gesture/back). One-line fix: use `findFood`; the screen already has a graceful "No reference photos available" state that would then be reached. No spec requires custom foods in the portion guide (they have no photoRefs by design), and no BUILD_NOTES claim covers it, so this is polish, not a violation.
2. **`FavoritesRecentsScreen` (N13) resolves only seed ids**, so a favorited/recent custom food appears in Add Entry's Favorites/Recent tabs (which do the merged resolution) but is silently omitted from the N13 management screen. The BUILD_NOTES and in-app claims are explicitly scoped to Add Entry + Food Detail and remain true; same one-line `findFood` fix.
3. **`PlacementStepsScreen` retains local state across a same-session cross-track retake.** In the edge path "complete both tracks → Combined Summary → Adjust (review) → Retake," `nav.navigate('PlacementSteps')` can return to the still-mounted instance from the *other* track, carrying its `stepIndex`/`answers`; a recomputed tier could then mix stale answers. This is user-initiated (explicit Retake), placeholder-content (tier 1–3, TBD), and unreachable outside one continuous session — but a `nav.push` or a `useEffect` state reset keyed on `route.params.track` would close it. Relatedly, "Skip this step" on the final step computes `yesCount` from a stale closure; harmless today (skip records 'no', which `yesCount` ignores) but fragile.
4. Pass-1 non-blocking observations (raw token-copy hexes outside `tokens.ts`, `StatusBadge`/`Card` tint values, gold favorite star, no-op Account save, no exposed theme toggle) are unchanged and carry forward as recorded.

**Verdict: APPROVED.** All nine required fixes are genuinely made in source — including the two state/flow fixes (3 and 4), which were traced through actual navigation params, stack ordering, and reducer dispatches rather than taken from BUILD_NOTES — and everything on the pass-1 "What passes" list is intact and unthinned. The typecheck claim is verified by the orchestrator's independent run (this session had no shell), and BUILD_NOTES now tells the truth end to end, including about its own prior falsehood. This was the final gate: the pipeline is complete, with the three non-blocking polish items above recorded for any future cleanup pass.

## 2026-07-12 — Stage: sitemap (RE-REVIEW after auth reorder) — Verdict: APPROVED

**Reviewed output:** `docs/sitemap.md` (edited after pass-2 approval: Sign Up/Log In moved from immediately after Splash to immediately before Onboarding Complete, per user feedback that onboarding should precede account creation)
**Dependencies:** `research-product` (approved, pass 2) → `research/product-research.md`. Also checked item-by-item against the pass-2 approval's preservation record.

### Change isolation independently verified

Rather than trusting the stated change list, the edit was verified via `git diff 50e9dd0..HEAD -- docs/sitemap.md` (pass-2-approved commit vs. current). Exactly three changes exist, matching the stated scope:
1. §1: the Sign Up/Log In bullet (email+password; forgot-password sub-flow — both retained) moved from after Splash to after the conditional Workout Placement Assessment block, immediately before Onboarding Complete, with an explicit rationale (ask for the account at the point where there is real progress worth saving) and a returning-user short-circuit (Splash → Log In skips the onboarding steps).
2. §2: the pre-nav line rewritten to the new order, including the returning-user parenthetical. Notably the old line's "Auth" label was replaced by "Sign Up/Log In" — no stale "Splash → Auth" reference survives anywhere (grep-verified: the only auth mentions in the file are the moved bullet, the new §2 line, traceability row 12, and the new flag).
3. §4: new "Open question this reorder introduces" flag on pre-auth local state handling.

Nothing else changed — the pass-2 preservation list (five-tab nav, all nutrition/workout/progress/settings screens, per-track placement structure, portion-photo surfaces, modal inventory, traceability rows, deferred list, Open Question 1 placeholder discipline) is intact by diff, not by re-reading impression.

### Internal consistency of the reorder: holds

- **§1 vs. §2:** the §2 pre-nav sequence (Splash → Profile Setup → Goal Setup → Region Preference → Module Interest → conditional per-track Placement Assessment → Sign Up/Log In → Welcome) matches the §1 screen-list order exactly, including the assessment sub-structure.
- **Returning-user path:** stated consistently in both places (§1 "short-circuit straight here from Splash"; §2 "Splash → Log In short-circuits straight past the onboarding steps").
- **Traceability table:** row 12 lists Sign Up/Log In among Req 12's screens without any order dependence; no other row references onboarding order. The table still holds as written.
- **New flag:** names exactly the five screens now pre-auth (Profile Setup, Goal & Target Setup, Region & Cuisine Preference, Module Interest, Workout Placement Assessment) — matches §1 precisely — and delegates the abandon-before-sign-up recoverability question to `user-flow-designer`, which is a real stage in `pipeline/state.json` (depends_on sitemap, output `docs/user-flows.md`), so the delegation target exists.

### Traceability to research-product: unaffected, one point checked hard

- Nothing in the research's 13 Core Feature Requirements, Constraints & Risks, or recommendations mandates auth-first ordering. Req 12 (single account tying nutrition + training) remains satisfied: Sign Up/Log In still precedes Onboarding Complete and the tab bar, so every in-app entity (diary entries, placement results, weight log) is created under an account; only onboarding answers are temporarily local, and the new flag addresses exactly that window.
- The one requirement genuinely touched by the reorder is **Req 9** (placement assessment): its results are now generated pre-account. The flag covers this explicitly (answers "held locally... then attached to the account at Sign Up"), and the assessment's per-track/deferral structure — the pass-2 fix — is untouched. Deferred placement re-entry from Skill Tree Home is post-auth and unaffected.
- Req 5 (offline-first) is, if anything, reinforced: local-first pre-auth state is consistent with the research's do-not-assume-always-online constraint.

### Non-blocking observations (carry-forwards for `user-flow-designer`, not rejection grounds)

1. **Whether the returning-user log-in path passes through Onboarding Complete/Welcome Summary or lands directly on the tab bar** is unspecified. Flow-level; resolve explicitly.
2. **Whether Sign Up is a hard wall** (no guest path into the tab bar) is implied by the sequential pre-nav structure but not stated. Given Req 12's single-account premise and the sync model, a hard wall before Welcome is the coherent reading; if user-flows wants a guest/skip mode, that contradicts Req 12 and must come back as a sitemap change, not be decided silently at flow level.
3. **Log-in on a device holding partial pre-auth onboarding state** (returning user starts onboarding, then logs into an existing account mid-way): what happens to the local answers vs. the account's stored profile is a conflict-resolution flow decision — same family as the flag's abandon question; resolve alongside it.

**Verdict: APPROVED.** The reorder is internally consistent, fully traced, additive-plus-move only, and honestly flags the one new open question at the right level of the pipeline. `user-flows` is unblocked on the sitemap side; the pass-2 carry-forwards (placeholder discipline, portion-photo as first-class, per-track/deferrable placement) remain in force, now joined by the three flow-level items above.

## 2026-07-12 — Stage: user-flows — Verdict: REJECTED

**Reviewed output:** `docs/user-flows.md`
**Dependencies:** `sitemap` (approved, pass 3) → `docs/sitemap.md`. Also checked against the three flow-level carry-forwards from the sitemap re-review (2026-07-12 entry above). Not a code stage — no tsc/screenshot verification applicable; this was a traceability, completeness, and internal-consistency review, with every "per the sitemap" claim grep-verified against the actual sitemap text.

### What passes (approved as-is — must NOT be thinned in revision)

**Screen coverage: complete.** Every screen in the sitemap's §1 list traces to a flow: all eight pre-nav screens including all placement sub-screens (A1–A9), Home (F1), all 13 Nutrition surfaces (B1–B7, including Portion Reference Guide as its own flow B2 and Food Search Results/Confirm & Log inside B1), all 8 Workout surfaces (C1–C5), all 3 Progress surfaces (D1–D3), all 8 Profile/Settings surfaces (E1–E8), and all six modals from the sitemap's modal inventory (Add Entry sheet in B1, Edit/Delete in B4, Portion Reference Guide in B2, Log Attempt in C2, Mastery Gate Confirmation in C2, Quick-add Weight in D2/F1). Checked screen-by-screen against the sitemap, not by impression.

**State coverage: systematically present.** The six common patterns (CP-OFFLINE, CP-NETFAIL, CP-VALIDATION, CP-EMPTY-SEARCH, CP-MODAL-BACKOUT, CP-PERMDENY) are well-factored and correctly referenced. Every form screen has validation + interruption rules; every list has empty/first-run states distinguished from zero-result states; async loads (Splash session check, Home cache-first, search, image load, OAuth) all have loading/degraded states; Req 4's "no data ≠ zero" rule is applied correctly on B5/F1 including trend-gap (not zero-point) rendering, and Req 5 offline queuing is applied at every write with E3 explicitly named as the queues' terminal surface.

**All three carry-forwards resolved explicitly, at the right level:**
1. §0.1 pre-auth draft persistence (commit-on-Continue, resume-at-first-incomplete-screen on relaunch, recoverable-not-reset with reasoning tied to the sitemap's own rationale) and the log-in-with-unsynced-draft conflict (field-level server-wins-where-present merge, with the stranding failure mode of the simpler alternative argued concretely, and honestly labeled a reasoned default).
2. §0.2 returning-user landing: straight to Home, no Welcome-back screen, transient sync banner only — reasoned, labeled as default.
3. §0.3 Sign Up hard wall: explicit no-guest/no-skip, tied to Req 12's single-account premise, with the required note that adding a guest mode later is a sitemap-level decision requiring new screens/data model — exactly what the carry-forward demanded.

**Fork/join points explicit, not hand-waved:** Module Interest fork/join (A2, incl. the A2a coming-soon branch with a flagged reactivation default), per-track placement fork/join with deferral semantics and the "opaque draft bundle" join at Sign Up (A3), pre-auth draft attaching at Sign Up incl. a signup/attach partial-failure atomicity default (A4), deferred-placement re-entry with never-opted-in ≡ deferred convergence (A9), the D1 cross-module data-merge branches, and Home quick-action rejoin rules (F1).

**Honesty discipline (mostly):** Section G consolidates 15 reasoned defaults, each genuinely flagged inline where introduced. Placeholder discipline holds — no invented node names, thresholds, or assessment content.

### Rejection grounds (narrow, concrete)

1. **Internal routing contradiction on the returning-user short-circuit — the exact area of carry-forward #2.** A1's trigger ("no cached account session and no local onboarding draft") routes Splash → Profile Setup. A7b step 1 states the *identical* precondition ("Splash finds no cached session and no in-progress local onboarding draft") and routes → Sign Up/Log In "directly." E2 step 4 repeats A7b's version after logout. Both cannot be automatic. A7b's heading implies user action ("user actively logs in from Splash"), and the sitemap's own language ("can still short-circuit") implies a user *choice* — but the document never specifies where that choice lives (a "Log in" affordance on Splash? an "Already have an account?" link on Profile Setup?). screen-designer cannot build the pre-nav entry logic from this. Fix: specify the short-circuit's trigger mechanism and affordance location explicitly, make A1 step 1, A7b step 1, and E2 step 4 consistent with it, and note how a genuinely-fresh user vs. a logged-out returning device are (or are not) distinguished. Note the E2 case is subtler than A7b as written: a post-logout device *has* no draft and no session, so if Splash auto-routes anywhere it must not strand a logged-out user in Profile Setup, nor force a brand-new user through a login wall — which is precisely why the mechanism must be stated, not implied.
2. **Fabricated sitemap quote (A3, single-track happy path):** the document quotes, in quotation marks attributed "per sitemap," the sentence "If only one track was chosen, this step is skipped." That sentence does not exist anywhere in `docs/sitemap.md` (grep-verified). The behavioral inference is correct (the sitemap conditions Track Selection on "if the user opted into both tracks"), but presenting an inference as a verbatim quote is a false internal claim — the same pattern rejected in research-design pass 2, design-system pass 1, and build pass 1. Fix: restate as an inference from the sitemap's conditional wording, without quotation marks.
3. **"Per the sitemap's per-step actions" (A3, error states):** the sitemap defines no per-step actions for Assessment Steps at all — no "Skip this step" action exists there. The skip-a-step-recorded-as-skipped behavior is a reasoned default this document invented (a fine one), and must be labeled as such (and added to Section G), not attributed to the sitemap.
4. **Cached-session claims mis-cited to the sitemap (§0.2 and A7a):** "app already logged in, Splash routes straight to Home per the sitemap's own description" and "a cached session is a valid, non-error state per the sitemap" — the sitemap never mentions cached sessions or any Splash→Home routing; its only returning-user statement is the Splash→Log In short-circuit. The behavior itself is sensible and unobjectionable; the citation is false. Fix: relabel both as this document's own reasoned defaults (Section G entry), consistent with how §0.1–0.3 already do this correctly.

### Non-blocking observations (fix optional, not rejection grounds)

- C1 uses the five-state node vocabulary (locked/unlocked/in-progress/completed/mastered) where the sitemap names only three (locked/in-progress/mastered). This matches the *approved* design system's SS1.4 and the approved screens conventions, so it is the right call — but since this stage's only dependency is the sitemap, a one-line note acknowledging the vocabulary comes from the approved design system would make the trace honest.
- Auth submits (A4/A5/A6) specify failure handling but not an in-flight submitting state (disabled button/pending affordance); harmless to leave to screen-designer, worth one line if touched anyway.
- The §0.1 "Start over" affordance is a new flow-level affordance not in the sitemap; it is within flow remit but is not listed in Section G — consider adding it.

**Verdict: REJECTED.** Fix items 1–4 only. Everything under "What passes" — the full flow set, the six common patterns, §0.1–0.3 resolutions, all fork/join treatments, and Section G — is approved as-is and must not be restructured or thinned. Item 1 requires real design of the short-circuit trigger; items 2–4 are attribution corrections that change no behavior.

## 2026-07-12 — Stage: data-research — Verdict: REJECTED

Dependency checked: `research/product-research.md` (approved, pass 2). Outputs read in full: `data/nutrition/foods.json` (32 foods), `data/workouts/skill-tree.json` (73 nodes/exercises), `docs/data-sourcing.md`. Per the data-research review protocol, cited figures were independently spot-checked (well beyond the 3-5 minimum), all unit/portion conversions and Atwater (4/4/9) macro-vs-kcal consistency were recomputed by script, JSON validity and prerequisite-graph resolution were machine-verified, and the confidence/limitations honesty claims were traced field-by-field into the data files.

### What passes (approved as-is — do not thin in revision)

- **Structure and contract:** both JSONs parse; every food carries `confidence`, a `source` with name+URL, and at least one `householdPortion` with a gram equivalent; every one of the 73 thresholds carries `threshold.confidence`; all prerequisites (including the cross-line muscle-up `pull-5`+`dip-4` gate and the 4-way handstand-push-up compound gate) resolve to real node ids; zero `[Skill Node]`/`TBD` placeholders in actual content (the lone regex hit is the `_meta.purpose` sentence referencing the sitemap's old placeholders — verified harmless).
- **Spot-checks that verify (10+ figures):** all 8 USDA rows are genuinely accurate against FoodData Central from independent knowledge — chicken breast 165/31/3.6, boiled egg 155/13/1.1/11 (50g large), white rice 130 kcal with the correct 158g/cup, banana 89 kcal / K 358mg / vitC 8.7mg (118g medium), peanut butter 588 kcal (16g/tbsp), whole milk 61 kcal / Ca 113mg (244g/cup), whole-wheat bread 247 kcal (28g slice), cooked oats 71 kcal (234g/cup). Nyama choma 144/27/4 matches USDA roasted goat (~143/27.1/3.0) almost exactly. Garri 363 kcal/100g dry is order-of-magnitude correct for roasted cassava granules. Jollof, akara, moin-moin, groundnut soup, and mandazi are Atwater-consistent within ~2%. Ogbono's documented 250g→100g derivation computes correctly (350/2.5=140; 10/25/15g ÷2.5 ✓).
- **Regional and unit coverage vs research-product/sitemap:** Nigeria 10 / Ghana 5 / Kenya 9 / Diaspora 8 matches the approved priority markets plus the diaspora persona (thinness of the Western set honestly disclosed as intentional). Swallow-style household units (eba wrap, pounded-yam wrap, fufu wrap), ladles, cups, balls, slices, skewers all present with gram equivalents — exactly what Ingredient/Composite Meal Detail's household-unit picker needs. Composite local dishes (Req 2) well represented.
- **Skill tree content quality:** real node names and real orderings throughout. Push/pull/leg-raise step sequences match Convict Conditioning's published Big Six steps; the pistol regression (box → assisted/counterbalance → full) and the muscle-up strength gate (10 strict pull-ups + 8-10 strict dips) are genuinely convergent across the cited coaching sources; the handstand-push-up compound gate (30s freestanding + 20 push-ups + 10 pull-ups + 15s L-sit) correctly turns docs/idea.md's illustrative example into a cited threshold. Pilates exercises are all genuine classical mat exercises with canonical Basic (Hundred → Roll-Up → Single Leg Circle → Rolling Like a Ball → SLS → DLS → Spine Stretch Forward) and Advanced (Teaser, Swimming, Control Balance, Boomerang, Push-Up) tier assignments; the tier-level gating model honestly reflects how classical Pilates is taught rather than fabricating an exercise-by-exercise chain for positions 5-34.
- **Honesty of the headline limitations:** the no-direct-FCT-access limitation (WAFCT/Nigerian FCT/KFCT PDFs blocked, values one step removed via compilations that cite them) is disclosed in three places including `foods.json` `_meta.knownLimitation`, and `direct-fct` is honestly *defined* as via-secondary-compilation. The doc's stated coverage gaps are all genuinely reflected in the data: banku macros are `null` (not invented), terere iron is `null` with the qualitative-only reason recorded, suya iron/zinc are `null`, groundnut soup carries its widest-spread low-confidence flag in `valueSpread`. The skill tree's `_meta.confidenceStatement` explicitly does NOT claim Open Question 1 (expert validation) discharged. This is the honest-labeling behavior the agent spec demands, and it is largely done well.
- **Methodology note verified:** Atwater deviations of ~10-20% on leafy vegetables/fruit are NOT defects per se — genuine USDA rows (banana 89 vs 98.3 computed, terere/amaranth) deviate the same way because carbs-by-difference include fiber. The rejection items below are *self*-contradictions, not Atwater pedantry.

### Why rejected (fix list — each item concrete)

1. **Egusi soup contradicts its own valueSpread (agent-spec process step 4: "no two sourced figures silently contradict each other").** `ng-egusi-soup` states 182 kcal/100g with a 250g ladle portion → 455 kcal/serving, while its own `valueSpread` says per-250g trackers show 350-400 kcal and calls this "consistent with the per-100g figure used here." 455 is outside 350-400; the claim is arithmetically false. Either lower the per-100g point estimate into the range implied by the cited serving figures (~140-160 kcal/100g) or rewrite the valueSpread honestly (the chosen value sits ABOVE the quoted serving-level range). Its macro panel (14.3P/8C/13.2F → 208 kcal Atwater vs 182 stated) should be reconciled in the same pass.
2. **Mandazi portion math contradicts its own valueSpread.** `ke-mandazi`: 320 kcal/100g × the stated 60g piece = 192 kcal/piece, but the `valueSpread` claims sources report "roughly 292-320 kcal per piece." All three numbers cannot be true: either the piece weight is wrong (a 292-320 kcal piece at 320 kcal/100g weighs ~90-100g) or the per-100g/per-piece figures were conflated. Also remove or source-flag the "~28 kcal/piece vegan mahamri" aside — a fried-dough piece at 28 kcal is not physically plausible and looks like an uncaught source artifact.
3. **Req-4 micronutrient coverage fails traceably, and the limitations section is not honest about it.** research-product Core Feature Req 4 names iron, zinc, calcium, vitamin A, folate, B12 "at minimum"; the sitemap builds a Micronutrient Detail screen (weekly trends for exactly those six), a Home micronutrient snapshot, and a Daily Summary micronutrient panel on them. Machine count across all 32 foods: B12 appears NOWHERE (zero entries — the B12 trend screen can never render data), zinc 1 non-null, vitamin A 2, folate 1; only 11 of 32 foods have any non-null micronutrient at all. Critically, the 8 USDA-confidence items carry near-empty micronutrient objects even though the cited source (FoodData Central) publishes full panels for exactly those foods — egg (B12 ~1.1µg, vitA, folate, iron, zinc), whole milk (B12 ~0.45µg, vitA), chicken breast (B12, zinc), whole-wheat bread (iron, folate, zinc). That is an omission, not a source-availability gap, and `docs/data-sourcing.md`'s limitations section (which itemizes banku/terere/groundnut-soup/SA-Ethiopia) never states that the six deficiency-relevant nutrients the product is differentiated on are mostly absent. Fix: populate the six Req-4 nutrients wherever an already-cited source unambiguously carries them (all USDA items at minimum; the FitNigerian FCT-derived pages typically carry micros too), and add an honest per-nutrient coverage statement (e.g. "B12: n of 32 foods") to the limitations.
4. **`compiled-estimate` label breaks its own legend on two entries.** The `_meta.confidenceLegend` defines `compiled-estimate` as "flagged with the range and the reason for variance," but `gh-waakye` and `gh-fufu` carry the label with no `valueSpread` at all. Add the observed range/reason to both, or (if only one source was found) relabel per what actually happened.
5. **Skill-tree `medium` confidence claims a provenance the numbers don't have.** The legend defines `medium` as "sourced to a single well-documented named system (most often Convict Conditioning)," yet the rep volumes on the CC-ordered lines are not CC's published progression standards — e.g. CC's step-1 wall push-up progression standard is 3×50 (its standards are systematically far higher), vs 2×20 here. Only the push line's `sourceSystem` discloses "+ general convergent volume standard for progressing steps"; the pull/squat/core lines claim bare "Convict Conditioning progression (10 steps)" while carrying non-CC volumes labeled `medium`. Per the agent spec (estimates must be labeled estimates; both silent overclaiming and silent placeholder-avoidance are failures), either (a) adopt CC's actual published standards for CC-attributed thresholds, or (b) keep the reasoned volumes but relabel them under the file's own `estimate` tier (or redefine `medium` explicitly as "ordering sourced to the named system; volume is a reasoned generic standard") and extend the push line's disclosure to every CC line. This is the same claimed-provenance-vs-actual-content pattern this pipeline has rejected four times before, in milder form because the push line partially discloses it.

### Fix-alongside (minor, not independently blocking)

- `pil-swimming` uses threshold type `duration_seconds` while every calisthenics hold uses `hold_seconds` — unify the key so app-builder doesn't need two code paths for one semantic.
- `ke-chapati`: 300 kcal/100g is hard to reconcile with its own macro panel (7.5P/50C/2.5F → 252.5 Atwater; fiber explains ~15 kcal of the 47.5 gap) and 2.5g fat/100g is implausibly low for an oil-cooked flatbread at 300 kcal/100g; also "1 piece (medium) = 40g" is small for a Kenyan chapati. Re-verify against the cited source while fixing items 1-2.
- `docs/data-sourcing.md`'s "first four exercises of the master sequence" — in the original 34, position 3 is Roll-Over (an advanced exercise); the corroborated Hundred→Roll-Up→Single Leg Circle→Rolling Like a Ball order is the Basic-mat *teaching* order. Phrase precisely.
- Pilates track uses a `tiers[].exercises` shape vs calisthenics `lines[].nodes` — documented and source-faithful, fine, but screens/build should be pointed explicitly at `gatingModel` so the two shapes don't surprise them.

Everything under "What passes" is explicitly approved as-is and must not be thinned in revision — in particular, do not delete honest nulls to make item 3's counts look better; item 3 is fixed by adding sourced values and honest coverage statements, not by removing fields.

## 2026-07-12 — Stage: user-flows (SECOND PASS) — Verdict: REJECTED

**Reviewed output:** `docs/user-flows.md` (revision after first-pass rejection, commit c13d067)
**Dependencies:** `sitemap` (approved, pass 3) → `docs/sitemap.md`. Checked against the first-pass four-item fix list; every reattributed or newly quoted "sitemap" claim independently re-grepped against `docs/sitemap.md` (not taken from the fix summary), and change isolation verified via `git diff 2cd5088 c13d067 -- docs/user-flows.md`.

### First-pass fix list: three of four verified done; fix 1 left a residual contradiction

| Required change (pass 1) | Status in revision |
|---|---|
| 1. Specify the short-circuit trigger/affordance; make A1, A7b, E2 consistent | **Done at the four named locations, but incomplete overall — see rejection ground below.** §0.2 now defines a three-state Splash check (cached session / local draft / new **device account-history marker**) with an explicit priority order; the marker's full lifecycle is specified (set on first successful Sign Up or Log In per A4/A5, left untouched by ordinary Log Out, cleared only by uninstall/clear-app-data/confirmed account deletion); the manual "Already have an account? Log in" affordance is placed on Profile Setup and persists across every pre-auth screen, with the draft-preserving jump to A5 specified; fresh-device vs. logged-out-returning-device distinction is exactly the marker. A1's trigger now requires marker-absent; A7b step 1 requires marker-set; E2 step 4 states log-out preserves the marker while account deletion (E2 deletion path) purges it and routes to A1. All four locations verified mutually consistent, and the whole mechanism is honestly labeled a reasoned default (Section G #16) with the sitemap quoted accurately ("can still short-circuit straight here from Splash" — verbatim, sitemap line 36). |
| 2. Fabricated "per sitemap" quote in A3 | **Done.** The nonexistent sentence "If only one track was chosen, this step is skipped" is gone; the skip behavior is now explicitly "this document's own inference, not a verbatim sitemap statement," reasoned from the sitemap's conditional wording, which IS quoted verbatim this time (grep count 1 in `docs/sitemap.md` line 28), and recorded as Section G #17. |
| 3. "Per the sitemap's per-step actions" | **Done.** Now states "the sitemap defines no per-step actions for Assessment Steps at all" (re-verified: it doesn't) and labels skip-recorded-as-skipped as this document's own reasoned default, Section G #18. |
| 4. Cached-session claims mis-cited to the sitemap (§0.2, A7a) | **Done in both places.** Each now states the cached-session-valid-state and Splash→Home routing are this document's own reasoned defaults and that "the sitemap never mentions cached sessions" — re-verified by grep: the only "session" hits in the sitemap are the Workout Session Log/Calendar screens. Folded into Section G #16. |

### Preservation check

`git diff 2cd5088 c13d067` over `docs/user-flows.md` shows every hunk is within the four fix areas or directly supporting additions (Section G #16–18; marker cross-references in A1 step 7, E2 deletion step 3, E2 log-out step 4). Nothing from the pass-1 "what passes" list was removed, restructured, or thinned. The three untouched remaining "per the sitemap" citations (A2a coming-soon rollout note; D2/F1 "Home has no visible weight card") were re-verified true against the sitemap.

### Rejection ground (single, narrow, mechanical)

**§0.1 steps 2–3 still state the OLD two-state Splash routing, directly contradicting the new mechanism.** §0.1 step 2: "Splash's session-restore check runs **two** checks in this order" (session, then draft); step 3: "**If neither exists, this is a genuine first launch → Flow A1 from the top.**" Forty lines later, §0.2's mechanism note says the same check "evaluates **three** independent pieces of local device state, **not two**," and routes the no-session/no-draft/marker-set case (most commonly a just-logged-out device per E2 — the precise subtle case pass 1 flagged) to Sign Up/Log In, not A1. §0.1 step 3 also now contradicts Flow A1's own revised trigger, which requires the marker to be absent. Both statements cannot be true; this is the identical-precondition-routes-two-places defect that was rejection ground #1, surviving un-reconciled in a fifth location the fix didn't touch — and §0.1 is the first substantive section a screen-designer reads. Per this pipeline's precedent (design-system pass 2), a half-applied fix leaving a live internal contradiction is rejected narrowly rather than waved through.

**Fix (mechanical, two lines):** update §0.1 step 2 to reference the three-state check (or say "the checks relevant to draft recovery, see §0.2 for the full three-state mechanism"), and correct step 3 to: if neither session nor draft exists, routing proceeds per §0.2's marker check — marker set → Sign Up/Log In (A7b), marker absent → genuine first launch (A1). Change nothing else.

**Fix alongside (minor):** §0.2's marker description claims its clearing conditions are "the same lifecycle as the onboarding draft described in §0.1" — not accurate: the draft is also cleared on successful attachment at Sign Up (A1 step 8) and on explicit "Start over," while the marker is not. The marker's own clearing conditions are exhaustively and correctly stated, so no behavioral ambiguity exists, but the analogy clause is a false internal cross-claim — drop or qualify it.

### Non-blocking observations

- The new A3 quote truncates the sitemap's sentence before its final word ("...or both **now** (either order...)") without an ellipsis. Meaning is unchanged and the quoted span is verbatim; add an ellipsis if touching the file anyway.
- Pass 1's non-blocking observations (C1 five-state vocabulary note, auth in-flight submitting state, §0.1 "Start over" not in Section G) remain unaddressed and remain non-blocking.

**Verdict: REJECTED** on the single §0.1 residual plus the minor lifecycle-analogy correction. Everything else — the entire marker mechanism, all four pass-1 fixes as applied, Section G #16–18, and the full pass-1 "what passes" list — is verified correct and approved as-is; do not restructure or thin anything while making the two-line fix.

## 2026-07-12 — Stage: design-system (CODE STAGE, first pass under the new contract) — Verdict: REJECTED

**Reviewed output:** `design-system/` (real Expo/RN TypeScript workspace package `@fit-and-fed/design-system`) plus `docs/design-system.md` (short rationale doc).
**Dependencies:** `research-design` (approved, pass 3 — status re-confirmed in `pipeline/state.json`) → `research/design-research.md`. Also checked against the five design-system carry-forwards recorded in the research-design third-pass approval, `.claude/agents/design-system-architect.md`'s output contract, and (as non-binding reference) `docs/reference/legacy-design-system.md`'s already-vetted color/contrast resolutions.

### Independent verification actually performed (not taken from the agent's claims)

- **Typecheck re-run by this reviewer:** `cd design-system && npx tsc --noEmit` → exit 0.
- **Web export re-run:** `npx expo export --platform web` → succeeded (bundle + dist produced).
- **Visual verification done:** dist served on `127.0.0.1:8934`, ComponentGallery screenshotted with Playwright/Chromium at 400px width across all 12 sections, in BOTH light and dark modes (including the calisthenics rest phase and the Pilates player in dark mode), and every screenshot was actually inspected. States not screenshotted: the two `complete`-phase summaries (verified in source instead — display-size headline + NodeStateBadge; disclosed here, not skipped silently). The portion-photo tiles rendered their failed-to-load fallback because the sandbox blocks external images — which incidentally proved the offline fallback state works as documented (icon + caption, no error toast).
- **Contrast independently recomputed** (WCAG relative-luminance): every ratio stated in `tokens.ts` comments is accurate — goldMuted `#7A4E12`: 6.50/5.79/3.99 vs bg/surface/track; node.unlocked `#A85F12`: 4.41/3.93; base gold `#D9A441`: 2.04 (claimed ~2.1). Pilates in-workout numeral (charcoal `#4A4A44` on `#F7F3EE`) = 8.07:1, clearing the research's higher-than-minimum in-workout target; calisthenics timer (white on `#17181A`) = 17.8:1.
- **Node-vocabulary exclusivity spot-checked in source, not trusted:** the five node hexes (`#8A8578`, `#A85F12`, `#3B7EA8`, `#3D8B5C`, `#A8452A`) are the exact pass-3-approved values; grep confirms zero occurrences of any node hex outside `tokens.ts`, zero occurrences of the banned superseded drafts `#C98A2E`/`#E0BE7C` anywhere, and the only raw hex bypassing tokens in any component is a `shadowColor: '#000'` in ToggleSwitch (hygiene). The claimed runtime assertion is real: `assertNodeColorsAreExclusive()` in `tokens.ts` throws in `__DEV__` if any node hex collides with brand/semantic/macro values, and the only consumers of node colors are SkillNode/NodeStateBadge/the gallery's swatch section — no misuse via token references either. (Gap in the guard: it does not include the neutrals — manually verified no collision today; cheap to add.)

### What passes (verified in source AND on screen, not vibes)

- **Agent contract shape: complete.** Root `package.json` has the `workspaces` field; `design-system/` is independently runnable (its own Expo app booting ComponentGallery via `main`, while `exports` points consumers at the library barrel — the dual-entry trick is documented in the package.json description); typed token exports; every component implements its documented states in code; gallery renders all of them.
- **Carry-forward 1 (fixed exclusive gamification vocabulary):** done and enforced — see verification above. NodeStateBadge exists as the semantic-free node chip; StatusBadge is deliberately separate for system state; both render icon+label, never color alone.
- **Carry-forward 3 (household-unit-first portion entry):** done — HouseholdUnitPortionPicker defaults to unit chips with gram entry demoted to a secondary "advanced" link, no-conversion fallback state implemented; PortionPhotoReference is a first-class component with omit-when-absent and offline-fallback states, verified rendering.
- **Carry-forward 4 (cultural-identity decision):** resolved explicitly in `docs/design-system.md` (Option 2, restrained — color temperature + photography discipline, no decorative motif layer), with the tokenism reasoning and revisit-with-community-input note carried from the legacy resolution.
- **Carry-forward 5 (low-bandwidth/low-end constraints):** honored structurally — solid colors, system font stack, Card uses border not shadow, no animation library, reduced-motion consulted by every animated element (SessionPlayer celebration, BottomSheet/modal transitions, SkillNode press ring, ToggleSwitch), haptics lazy-required so web bundles never pull native code.
- **`docs/design-system.md` stays short and rationale-only as the contract requires:** 72 lines, zero hex values, zero prop tables, explicit pointers to `tokens.ts`/`components/` as source of truth; legacy departures (48pt floor, six additive components) documented with reasons. Checked line-by-line: every claim in it is true of the code.
- **The six previously-flagged component gaps are all genuinely filled:** SegmentedControl, SingleSelectChips (radio semantics, checkmark + fill, error state), ToggleSwitch (track/knob geometry, not color-only), CalendarDatePicker (month nav, marked dates, selected/today states), TrendChart (solid stroke, real empty state), ListRow (title/subtitle/badge/chevron/disabled). All exported from the barrel and demoed in the gallery. (SegmentedControl has a touch-target defect — gap 2 below — but the component itself is real.)
- **Rendered aesthetic matches the research direction:** the light gallery reads warm/clean (off-white `#F7F3EE` ground, terracotta primary, restrained accents, generous spacing) — verified by eye against the "warm, clean, legible / not fintech blue-green, not clinical pastel" direction; node states are visually distinct with icon pairing; macro rings' over-limit state swaps to semantic error AND shows "Over limit" text (color never alone), confirmed on screen.

### Concrete gaps (reasons for rejection)

1. **The Pilates session-player preset leaks app-wide dark mode, contradicting its own code comment — and the resulting render fails the system's own 3:1 progress-fill rule.** `SessionPlayer.tsx` line ~79-82 states: "Pilates is always light/muted, even if the user has app-wide dark mode on. Both are intentional per the research's tonal-flex requirement" — but the calisthenics branch forces `darkNeutrals.background` while the Pilates branch uses theme-dependent `theme.neutrals.background`. Verified live: flipping the gallery to dark renders the Pilates player on `#17181A`, directly under a section header still reading "light, calm, audio-led," tonally indistinguishable from the calisthenics preset — erasing the research's tonal-flex requirement in dark mode. Worse, the goldMuted `#7A4E12` progress fill — whose token comment's "verified ≥3:1" figures are all light-surface pairings — recomputes to **2.48:1** vs the dark background and **1.56:1** vs its dark track (`theme.neutrals.border` `#3A3B3E` via ProgressBar), far below the 3:1 non-text floor the legacy system took two passes to enforce for exactly this indicator. This is the false-internal-claim pattern this pipeline has rejected four times. Fix is one line (mirror the calisthenics force: use `lightNeutrals` for the Pilates preset), plus re-verifying the timer/secondary colors that branch on `theme.neutrals` still resolve to the light set.
2. **SegmentedControl's touch targets measure 40px tall while its doc comment claims "Each segment meets the 44/48pt touch-target floor."** The segment Pressable sets `minHeight: theme.minTouchTarget - 8` (= 40) and the track's 4px padding is outside the pressable area. Measured in the rendered DOM via Playwright: every segment is exactly 40px tall (TabBar's tabs correctly measure 48). That is below the iOS 44pt floor the agent contract calls an App-Review rejection risk, below the package's own 48 floor, and another claim-vs-code falsehood. Fix: give the pressable the full floor (or compensating hitSlop) and make the comment true.
3. **(Minor, fix alongside) Two more interactive elements sit below the floor, and one comment over-claims:** (a) HouseholdUnitPortionPicker's "Enter exact weight instead" link is a bare caption-height Pressable with no minHeight/hitSlop; (b) CalendarDatePicker's month-label Pressable (jump-to-today) likewise; (c) the calendar-cell comment claims cells "always meet the touch-target floor via hit-slop," but with `hitSlop={4}` the guarantee fails below ~360px screen widths (at 320px: ~35px cell + 8 = 43px) — either compute hitSlop from measured cell size or soften "always" to the actual guarantee.

### Non-blocking observations (recorded, not rejection grounds)

1. `assertNodeColorsAreExclusive` omits the neutrals sets from its collision check; manually verified clean today — add them so the guard matches the legacy exclusivity bar it cites.
2. The gallery's demo strings use real exercise names and thresholds ("Full planche," "Handstand push-up unlocked," "Plank hold: 60s milestone"). These mirror the research document's own illustrative examples and exist only in the preview surface, so they don't breach Open Question 1 discipline here — but **screen-designer must not copy these strings into app content** (carry-forward).
3. NodeStateBadge renders its 12px label in the node color; the locked state computes ~2.97:1 on the tinted chip — defensible under the same inactive-state reasoning as the locked node itself, but worth a one-line comment.
4. The rest-timer's lock-screen/notification persistence (a build-stage behavior per the legacy spec) is not restated anywhere in this package or doc — record it as a build carry-forward so it isn't lost in the doc slimming.
5. SessionPlayer only shows the "Rest" caption when `subLabel` is provided (the label substitutes for it); harmless, but an app passing no subLabel gets an unlabeled countdown in rest phase.
6. `shadowColor: '#000'` raw hex in ToggleSwitch (hygiene; shadow color arguably outside the token palette's remit).

### Required changes for approval (narrow — everything under "What passes" must be preserved as-is)

1. Force the Pilates SessionPlayer preset onto the light neutrals in all its surfaces/text/track colors (mirroring how calisthenics forces dark), so the code matches its own comment, the tonal flex survives app-wide dark mode, and goldMuted's verified pairings are the only ones reachable. Verify by screenshotting the Pilates player with the gallery in dark mode.
2. Fix SegmentedControl's segment height to genuinely meet the 48px floor (or equivalent hitSlop) and correct the comment; re-measure.
3. Fix the three minor touch-target items (portion-picker advanced link, calendar month label, calendar-cell hitSlop guarantee/comment).
4. Re-run `npx tsc --noEmit` and `npx expo export --platform web` after the fixes (this review will re-run both regardless).

**Verdict: REJECTED** — narrowly. The package is structurally strong (tokens verified exclusive with a real runtime guard, all six gap components built, carry-forwards 1/3/4/5 genuinely done, doc contract met, typecheck and export verified clean by this reviewer), but both rejection gaps are code-contradicts-its-own-claim defects of the exact class this pipeline rejects, one of which produces a sub-2:1 progress indicator in a reachable mode. All fixes are mechanical and small. Screens stage remains blocked on this stage.

## 2026-07-12 — Stage: user-flows (THIRD PASS) — Verdict: APPROVED

**Reviewed output:** `docs/user-flows.md` (revision after second-pass rejection, commit 118b5c9)
**Dependencies:** `sitemap` (approved, pass 3 — status re-confirmed in `pipeline/state.json`) → `docs/sitemap.md`. Checked against the pass-2 two-item fix list; routing consistency re-verified directly in the document at all five locations (§0.1, §0.2, A1, A7b, E2), not taken from the fix agent's summary; change isolation verified via `git diff c13d067 HEAD -- docs/user-flows.md`.

### Pass-2 fix list: both items verified done

| Required change (pass 2) | Status in this revision |
|---|---|
| 1. §0.1 steps 2–3: replace the stale two-check routing with the three-check mechanism; just-logged-out device must route to A7b, not A1 | **Done.** §0.1 step 2 now explicitly evaluates "the same **three** independent pieces of local device state as §0.2's Mechanism note (cached account session, local onboarding draft, device account-history marker)" in the same priority order, and step 2(a) is upgraded from the vague "Flow A7 (returning user)" to the precise "Flow A7a (returning user), straight to Home" — matching §0.2 branch 1. Step 3 now states: no session + no draft → marker check; marker **set** ("most commonly a just-logged-out device, Flow E2" — the exact case the rejection named) → Sign Up / Log In (Flow A7b), "**not** Flow A1"; only marker-**also**-absent → genuine first launch → A1. |
| 2. §0.2 marker-lifecycle analogy ("same lifecycle as the onboarding draft" — false) | **Done, and accurately.** Replaced with: the marker has "a **narrower** set of clearing conditions than the onboarding draft in §0.1, which is also cleared on successful attachment to an account at Sign Up and on explicit 'Start over'; neither of those two events clears the marker." Verified against §0.1's own draft-lifecycle list (cleared on attachment / log-in supersession / Start-over-or-clear-data): both cited draft-only events are real, and neither clears the marker — Sign Up *sets* it (A1 step 7), Start-over doesn't touch it. The clause omits the log-in-supersession draft-clearing event, but makes no exhaustiveness claim and every statement in it is true; "narrower" is correct since the draft's clearing set is a strict superset of the marker's. Not a defect. |

### Routing consistency independently re-verified (the check the rejection was about)

Traced the no-session/no-draft/marker-set precondition through every location that routes it — all five now agree it lands on Sign Up / Log In (A7b), and A1 is reachable only with the marker absent:

- **§0.1 step 3** (line ~55): marker set → A7b, "not Flow A1"; marker absent → A1. ✓
- **§0.2 routing list** (lines ~164–174): four branches — session → Home (A7a); draft → resume; no-session/no-draft/marker-set → Sign Up/Log In (A7b's short-circuit); all-three-absent → Profile Setup (A1). ✓
- **A1 trigger** (line ~285): explicitly requires "no cached account session, no local onboarding draft, **and no device account-history marker set**"; happy-path step 1 restates all three absent; step 7 notes first successful auth sets the marker. ✓
- **A7b step 1** (line ~558): no session, no draft, marker set → Log In pre-selected, distinct from the manual fresh-device affordance. ✓
- **E2 log-out step 4** (line ~1118): marker left untouched → Splash's no-session state → A7b's short-circuit, with the just-logged-out reasoning spelled out. **E2 deletion step 3–4** (lines ~1094–1098): purge includes the marker → all three states absent → A1 fresh start. ✓
- **Section G #16** (line ~1328): still accurately records the whole mechanism as a reasoned default reconciling A1/A7b/E2. ✓

### Change isolation (per the fix agent's own two-spot claim)

`git diff c13d067 HEAD -- docs/user-flows.md` contains exactly two hunks — the §0.1 steps 2–3 rewrite and the §0.2 lifecycle clause — and nothing else. No unstaged changes to the file. Nothing from any prior "what passes" list was removed, restructured, or thinned. (Commit 118b5c9 also touches design-system files; those belong to the design-system stage's open fix list and are out of scope for this review.)

### Non-blocking observations (carried, unchanged)

Pass 2's non-blocking items remain non-blocking and unaddressed: the A3 quote's missing ellipsis before the truncated final word; C1's five-state vocabulary note; auth in-flight submitting state; §0.1 "Start over" not itemized in Section G.

**Verdict: APPROVED.** The internal contradiction is fully resolved; the document is now routing-consistent end to end and every sitemap attribution previously flagged has been verified honest. **This unblocks the screens stage on the user-flows side.** Screens remains blocked until `design-system` (rejected, pass 1 under the new real-code contract) and `data-research` (rejected) are also approved. Carry-forwards for screens from this stage: (1) build Splash's pre-nav entry logic from §0.2's three-check mechanism exactly (cached session → Home; draft → resume at first incomplete screen; marker-set → Sign Up/Log In with Log In pre-selected; all absent → Profile Setup); (2) the persistent "Already have an account? Log in" affordance on every pre-auth screen from Profile Setup onward, draft-preserving; (3) the six common patterns (CP-*) and Section G's 18 reasoned defaults are binding flow specs, not suggestions. Reminder to the orchestrator: run the screens merge-point review at effort xhigh, per the standing note.

## 2026-07-12 — Stage: data-research (SECOND PASS) — Verdict: APPROVED

**Reviewed outputs:** `data/nutrition/foods.json`, `data/workouts/skill-tree.json`, `docs/data-sourcing.md` (revision after first-pass rejection).
**Dependency:** `research-product` (approved, pass 2). Checked against the pass-1 five-item fix list and, item by item, the pass-1 "What passes / do-not-thin" preservation list. Every figure below was recomputed by this reviewer's own script, and change isolation was verified via `git diff` against the pass-1 commit (6fbeded/e3aa60e) — not taken from any fix summary.

### First-pass fix list: all five items verified DONE

| Required change (pass 1) | Status in revision |
|---|---|
| 1. Egusi self-contradiction | **Done, via the honest-rewrite branch.** per100g kept at 182 kcal; recomputed 250g ladle = 455.0 kcal, and the rewritten `valueSpread` now states exactly that (455 = 182 × 2.5, attributed to NutriScan's own 250g-bowl figure), explicitly acknowledges the previous "350-400 consistent" claim was arithmetically false, and replaces it with a real spread (lean ~400-450/250g ≈ 160-180/100g; palm-oil-heavy 600+/250g ≈ 240+/100g) with 182 placed at the lean-to-moderate end. Macro panel rescaled to 12.5P/7C/11.5F → Atwater 181.5 ≈ 182 stated (recomputed; the old 208-vs-182 mismatch is gone), with the rescale disclosed in-file rather than made silently. Pre-existing folate/iron micronutrients untouched (diff-verified). |
| 2. Mandazi portion math | **Done.** Recomputed: 320 kcal/100g × 60g piece = 192 kcal/piece, which is now exactly what the `valueSpread` states. The 292-320 figure is re-attributed to a per-100g basis (plausible for fried sweet dough) with an internally consistent 31g/99-kcal cross-check (99/31×100 = 319.4 ≈ 318 as claimed, recomputed). The physically impossible "~28 kcal/piece vegan mahamri" is genuinely removed as a data claim — the number now appears only inside an explicit retraction explaining it was an uncaught source artifact, with the variant deliberately left un-catalogued rather than assigned an invented value. That satisfies "remove or source-flag." |
| 3. Req-4 micronutrients + honest limitations | **Done on both halves.** (a) All 8 USDA/diaspora items now carry full B12/zinc/folate/vitamin-A/iron/calcium panels, diff-verified as the only micronutrient additions in the file. Spot-checked against this reviewer's own knowledge of FoodData Central: egg (Fe 1.2 / Zn 1.1 / folate 44 / vitA-RAE 149 / B12 1.1 / Ca 50), whole milk (Ca 113 / B12 0.45 / Zn 0.37 / vitA 46 / Fe 0), enriched white rice (Fe 1.2 / folate 58 with the enrichment correctly explained), chicken breast (Fe 0.4 — the modern FDC Foundation value, not the older SR 1.04 — Zn 0.9 / B12 0.3) — all match FDC within rounding. Honest zeros (e.g. B12=0 in rice/oats/banana/PB/bread) are explicitly documented in-file as "genuinely absent, not missing data." (b) The 24 African-sourced foods' honest nulls are untouched — machine-verified: suya iron/zinc null, terere iron null, banku macros null all still present; zero African foods gained invented micronutrients (egusi's folate/iron predate this revision, diff-confirmed). (c) `docs/data-sourcing.md` now carries a prominent, bolded limitations item (#4, "read this before wiring the Micronutrient Detail screen") whose per-nutrient counts this reviewer independently recomputed and confirmed EXACT: B12 8/32 (all diaspora, zero African), zinc 9/32 (8 + ke-githeri), vitamin A 10/32 (8 + ke-managu + ke-terere), folate 9/32 (8 + ng-egusi-soup), including the explicit screen-impact statement that the B12 trend renders nothing for African-dish-only logging days, and the explicit no-deletion/no-backfill statement. |
| 4. gh-waakye / gh-fufu compiled-estimate legend | **Done, with real content, not label churn.** Both now carry substantive `valueSpread` entries (waakye 167-194 kcal/100g with rice:beans/oil/accompaniment reasons and an arithmetically consistent second-source corroboration — 379.1/227g = 167.0/100g, recomputed; fufu ~150-300+ kcal/100g by base starch/prep with the 166 point placed and justified). Machine check: zero `compiled-estimate` entries in the file now lack a `valueSpread`. Both figures are order-of-magnitude plausible for the dishes. |
| 5. Skill-tree medium-confidence provenance | **Done, via option (b) exactly as offered.** `confidenceLegend.medium` is redefined explicitly (step ORDERING sourced to the named system; rep/hold VOLUME is a reasoned generalized standard, NOT CC's verbatim targets, with the 3×50-vs-2×20 wall-push-up example retained), and the substitution disclosure now appears in all four CC lines' `sourceSystem` fields — push, pull, squat-legs (correctly scoped to steps 1-7, with 8-10 excluded as genuinely cross-convergent), and core (correctly excluding the separately-sourced L-sit branch). Machine check: every line containing medium-confidence nodes carries the disclosure; dip and handstand lines (no medium nodes) correctly don't. Diff-verified the ONLY skill-tree changes are the legend + four sourceSystem strings — no thresholds, orderings, names, or prerequisites changed. `docs/data-sourcing.md` mirrors this as an explicit "Important provenance correction (this revision)" in methodology plus limitation #6. |

### Preservation check (diff-verified, per the do-not-thin instruction)

`git diff` against the pass-1 versions shows changes confined to: egusi macros/valueSpread, waakye/fufu valueSpread additions, mandazi valueSpread/source note, the 8 diaspora micronutrient panels + source notes, the skill-tree legend + four sourceSystem fields, and the data-sourcing.md methodology/limitations additions. Everything on the pass-1 "What passes" list re-verified intact: 32 foods (10/5/9/8 regional split unchanged), ogbono's 250g→100g derivation, groundnut soup's widest-spread flag, `_meta.knownLimitation`, the skill tree's non-discharge of Open Question 1, all 73 thresholds carrying confidence tags, and full prerequisite-graph resolution (re-run: zero unresolved ids). Critically, no honest nulls were deleted or backfilled to flatter the coverage counts.

### Verification limitation (disclosed)

Live re-fetch of the cited secondary sources (nutriscan.app, caloriqueapp.com) was attempted and blocked by this environment's proxy (CONNECT 403) — the same access limitation the data files themselves disclose. The rewritten citation claims were therefore checked for internal arithmetic consistency (all compute) and order-of-magnitude plausibility from independent knowledge (all plausible); the USDA panel values were checked against independent knowledge of FoodData Central (all match). The pass-1 finding that these secondary sources are real sites of the claimed type stands.

### Carry-forwards (non-blocking, inherited from pass-1's fix-alongside list — still unaddressed, do not block approval since they were declared non-blocking then)

1. `pil-swimming` still uses `duration_seconds` where every other hold uses `hold_seconds` — app-builder should normalize or handle both.
2. `ke-chapati` was not re-verified: 300 kcal/100g vs Atwater 252.5 (fat 2.5g/100g still implausibly low for an oil-cooked flatbread), 40g "medium" piece still small — flag for the nutrition-analyst review pass.
3. The "first four exercises of the master sequence" phrasing (doc + gatingModel) still elides Roll-Over (position 3 of the original 34); the corroborated order is the Basic-mat teaching order.
4. Screens/build should be pointed explicitly at `gatingModel` for the Pilates `tiers[].exercises` vs calisthenics `lines[].nodes` shape difference.
5. (New, trivial) Limitation #4 says "see limitation 1 below" — limitation 1 is above it; fix the word if the file is touched again.

**Verdict: APPROVED.** All five rejection grounds verifiably fixed with honest, arithmetically consistent content; nothing thinned; the dataset's biggest remaining weakness (African-food micronutrient coverage) is now loudly and accurately documented instead of hidden. Screens/build stages are unblocked on the data-research side; they must read `docs/data-sourcing.md` limitation #4 before wiring the Micronutrient Detail / B12-trend surfaces, and must treat skill-tree content as unvalidated-per-Open-Question-1 synthesis exactly as labeled.

## 2026-07-12 — Stage: design-system (SECOND PASS under the new code contract) — Verdict: APPROVED

**Reviewed output:** `design-system/` (revision after the 2026-07-12 pass-1 rejection). Revision delta isolated via `git diff 7b1a214 HEAD -- design-system/`: exactly six component files touched (SessionPlayer, SegmentedControl, CalendarDatePicker, HouseholdUnitPortionPicker, ProgressBar, ToggleSwitch), all within the fix list's scope.
**Dependencies:** `research-design` (approved, pass 3 — status re-confirmed in `pipeline/state.json`) → `research/design-research.md`, plus the pass-1 fix list and do-not-thin list above.

### Independent verification actually performed (nothing taken from the agent's claims)

- **Typecheck re-run by this reviewer:** `cd design-system && npx tsc --noEmit` → exit 0.
- **Web export re-run:** `npx expo export --platform web` → succeeded; dist served on `127.0.0.1:8934`.
- **Visual + DOM verification via Playwright/Chromium at 400px:** gallery loaded in BOTH light and dark app modes; the Pilates and calisthenics players, calendar, portion picker, and segmented controls screenshotted and inspected by eye; rendered colors and element heights measured from the live DOM (not inferred from source). Note: full-page screenshots only capture the viewport because RN-web scrolls in a nested container — per-section viewport shots were used for the changed areas; unchanged sections were inspected in pass 1 and are diff-verified untouched, so they were not re-shot.
- **Contrast recomputed by script** (WCAG relative luminance), against the colors actually measured in the rendered DOM, not the token comments.

### Pass-1 fix list: all four items verified DONE

| Required change (pass 1) | Status in revision |
|---|---|
| 1. Pilates SessionPlayer preset forced onto fixed light neutrals in all surfaces/text/track; verify by dark-mode screenshot | **Done and verified live.** Source: `SessionPlayer.tsx` now contains zero `theme.neutrals` references — every color in both branches comes from the fixed `darkNeutrals`/`lightNeutrals` constants (grep-verified). The mechanism is honest: `ProgressBar` gained an optional `trackColor` prop and `ToggleSwitch` gained optional `trackOffColor`/`knobColor`/`labelColor` overrides (both additive; defaults unchanged for every other consumer). Measured in the rendered DOM: with the gallery in DARK mode, the Pilates player background stays `rgb(247,243,238)` = `#F7F3EE`, the timer stays `#4A4A44`, and the progress bar renders fill `#7A4E12` on track `#C9C0B2` — identical to light mode; the calisthenics player symmetrically stays `#17181A` in light mode. Screenshots of the Pilates player in both app modes inspected: visually identical light/calm surface, dark-gold fill clearly visible against the light track; the tonal flex survives app-wide dark mode. **Contrast recomputed against the actually-rendered track:** goldMuted `#7A4E12` vs `#C9C0B2` = **3.99:1** (matches the token's documented figure), vs background `#F7F3EE` = 6.50:1 — both clear the 3:1 progress-fill floor. The old failing dark pairings (2.48:1 vs `#17181A`, 1.56:1 vs `#3A3B3E`) are now unreachable: recomputed and confirmed still-failing, but no code path can produce them. |
| 2. SegmentedControl segments genuinely ≥ the 48px floor; correct the comment; re-measure | **Done and re-measured.** `minHeight: theme.minTouchTarget - 8` → `minHeight: theme.minTouchTarget`. Playwright-measured every rendered segment across all seven gallery instances (Light/Dark 180×48, Active/Rest/Complete 120×48, Search/Recent 90×48) in both modes: **exactly 48px tall** (was 40 in pass 1). The rewritten comment ("the track's 4px padding sits around the segment row, not subtracted from it") now matches the measured render. |
| 3. Three minor touch-target items | **All three done, measured not just commented.** (a) Portion-picker "Enter exact weight instead" link: `minHeight: minTouchTarget` + `accessibilityRole`/label added — measured 48px tall in DOM. (b) Calendar month-label (jump-to-today) Pressable: same treatment — measured 97×48. (c) Calendar cells: the Pressable now fills the entire 7-column grid cell (measured 53×53 at 400px width, vs the old smaller disc) and `hitSlop` is computed from the `onLayout`-measured column width (`max(4, ceil((minTouchTarget - columnWidth)/2))`, worst-case `minTouchTarget` before first measurement so no frame renders under-floor); arithmetic checked — at a 320px screen (~41px columns) the computed slop yields a ~49px hit area, ≥ the 48 floor. The over-claiming "always... via hit-slop" comment is replaced by an accurate description of the measured-top-up guarantee. |
| 4. Re-run tsc and expo export | **Both re-run by this reviewer regardless** — exit 0 / success (above). |

### Preservation check (against pass 1's do-not-thin list)

`git diff 7b1a214 HEAD` for `docs/design-system.md`, `design-system/src/theme/` (incl. `tokens.ts`), `design-system/src/index.ts`, and `design-system/package.json` is **empty** — the doc contract, the full token set, the node-vocabulary hexes and `assertNodeColorsAreExclusive` guard, and the workspace/dual-entry packaging are byte-identical to the pass-1-reviewed state. Re-grepped anyway: superseded `#C98A2E`/`#E0BE7C` appear nowhere; the five node hexes appear only in `tokens.ts`. The six gap components (SegmentedControl, SingleSelectChips, ToggleSwitch, CalendarDatePicker, TrendChart, ListRow) are all still present — the only touched ones (SegmentedControl, CalendarDatePicker, ToggleSwitch) changed strictly within fix scope, and ToggleSwitch/ProgressBar changes are additive optional props. Carry-forwards 1/3/4/5 land in untouched files/doc. Nothing thinned.

### Non-blocking observations (recorded, not rejection grounds)

1. ToggleSwitch's disabled label still uses ambient `theme.neutrals.placeholder` even when `labelColor` is passed — unreachable in SessionPlayer (its toggle is never disabled), but a fixed-surface caller passing `disabled` elsewhere would leak the ambient theme on that one label. One-line guard if it ever matters.
2. Calendar cell hitSlop zones can overlap adjacent cells' slop at very narrow widths (standard RN slop behavior; topmost sibling wins) — acceptable, noted for completeness.
3. All pass-1 non-blocking items remain open as carry-forwards, notably: the exclusivity guard omits neutrals; **screen-designer must not copy the gallery's illustrative exercise strings into app content** (Open Question 1 discipline); rest-timer lock-screen/notification persistence is a build-stage carry-forward.

**Verdict: APPROVED.** Every pass-1 rejection item and fix-alongside is verified fixed in the live render, not just in comments; the two claim-vs-code falsehoods are gone and the new comments state what the code measurably does. **The screens stage is now fully unblocked — all four of its dependencies (sitemap, design-system, user-flows, data-research) are approved.** Standing recommendation reaffirmed: run the screens merge-point review at effort **xhigh** (four-way dependency reconciliation plus visual judgment is the hardest check in this pipeline).
